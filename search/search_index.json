{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"index.html","title":"Home","text":""},{"location":"index.html#welcome-to-cloudmesh-ai-vm","title":"Welcome to cloudmesh-ai-vm","text":"<p>`cloudmesh-ai-vm` is a unified Python CLI tool (`cmc`) designed to simplify the management of Virtual Machines (VMs) across a diverse set of providers. Whether you are using local virtualization for development or scaling in the cloud for production, `cmc` provides a consistent interface for the entire VM lifecycle.</p>"},{"location":"index.html#key-value-propositions","title":"Key Value Propositions","text":"<ul> <li>One Interface, Many Clouds: Stop learning different CLI tools for AWS, Azure, Multipass, and OpenStack. Use `cmc` for all of them.</li> <li>Automated Workflow: Built-in automatic naming and counter management to avoid name collisions when launching multiple VMs.</li> <li>Developer Focused: Designed for AI researchers and developers who need to spin up and tear down environments quickly across different infrastructures.</li> <li>Extensible: Based on the Provider Pattern, making it trivial to add support for new cloud services.</li> </ul>"},{"location":"index.html#supported-providers","title":"Supported Providers","text":"Category Providers Local Multipass, WSL2, VirtualBox OpenStack Jetstream, Chameleon Cloud Hyperscalers AWS EC2, Azure VMs, Google Compute Engine"},{"location":"index.html#quick-start","title":"Quick Start","text":"<ol> <li>Install the tool and its dependencies.</li> <li>Configure your credentials in `~/.config/cloudmesh/clouds.yaml`.</li> <li>Set your default provider:    ```bash    cmc vm set multipass    ```</li> <li>Launch your first VM:    ```bash    cmc vm start    ```</li> </ol> <p>See the Installation Guide to get started.</p>"},{"location":"CHANGELOG.html","title":"Changelog","text":""},{"location":"CHANGELOG.html#changelog","title":"Changelog","text":"<p>All notable changes to this project will be documented in this file.</p>"},{"location":"CHANGELOG.html#unreleased","title":"[Unreleased]","text":"<ul> <li>Initial project setup.</li> </ul>"},{"location":"api.html","title":"cloudmesh-ai-vm API Reference","text":""},{"location":"api.html#cloudmesh-ai-vm-api-reference","title":"cloudmesh-ai-vm API Reference","text":"<p>This page provides the API reference for the <code>cloudmesh-ai-vm</code> library.</p>"},{"location":"api.html#api-documentation","title":"API Documentation","text":""},{"location":"api.html#cloudmesh.ai.command.vm-attributes","title":"Attributes","text":""},{"location":"api.html#cloudmesh.ai.command.vm.CONFIG_PATH","title":"CONFIG_PATH  <code>module-attribute</code>","text":"<pre><code>CONFIG_PATH = os.path.expanduser('~/.config/cloudmesh/clouds.yaml')\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.entry_point","title":"entry_point  <code>module-attribute</code>","text":"<pre><code>entry_point = vm_group\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.factory","title":"factory  <code>module-attribute</code>","text":"<pre><code>factory = ProviderFactory()\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.logger","title":"logger  <code>module-attribute</code>","text":"<pre><code>logger = setup_logger('cloudmesh')\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.state","title":"state  <code>module-attribute</code>","text":"<pre><code>state = StateManager(CONFIG_PATH)\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm-classes","title":"Classes","text":""},{"location":"api.html#cloudmesh.ai.command.vm.AwsProvider","title":"AwsProvider","text":"<p>               Bases: <code>LibcloudManager</code></p> <p>AWS EC2 implementation of the LibcloudManager.</p> Source code in <code>src/cloudmesh/ai/vm/aws/AwsManager.py</code> <pre><code>class Provider(LibcloudManager):\n    \"\"\"\n    AWS EC2 implementation of the LibcloudManager.\n    \"\"\"\n\n    def __init__(self, config):\n        super().__init__(config, cloud_name=\"aws\")\n\n    def _get_driver(self):\n        cloud_config = self.get_cloud_config(\"aws\")\n        return AmazonEC2Driver(\n            access_key=cloud_config.get(\"access_key\"),\n            secret_key=cloud_config.get(\"secret_key\"),\n            region=cloud_config.get(\"region\", \"us-east-1\")\n        )\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.AzureProvider","title":"AzureProvider","text":"<p>               Bases: <code>LibcloudManager</code></p> <p>Azure implementation of the LibcloudManager.</p> Source code in <code>src/cloudmesh/ai/vm/azure/AzureManager.py</code> <pre><code>class Provider(LibcloudManager):\n    \"\"\"\n    Azure implementation of the LibcloudManager.\n    \"\"\"\n\n    def __init__(self, config):\n        super().__init__(config, cloud_name=\"azure\")\n\n    def _get_driver(self):\n        cloud_config = self.get_cloud_config(\"azure\")\n        return AzureDriver(\n            tenant_id=cloud_config.get(\"tenant_id\"),\n            subscription_id=cloud_config.get(\"subscription_id\"),\n            client_id=cloud_config.get(\"client_id\"),\n            client_secret=cloud_config.get(\"client_secret\")\n        )\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.ChameleonProvider","title":"ChameleonProvider","text":"<p>               Bases: <code>OpenstackManager</code></p> <p>Chameleon Cloud implementation of the OpenstackManager.</p> Source code in <code>src/cloudmesh/ai/vm/openstack/ChameleonManager.py</code> <pre><code>class Provider(OpenstackManager):\n    \"\"\"\n    Chameleon Cloud implementation of the OpenstackManager.\n    \"\"\"\n\n    def __init__(self, config):\n        super().__init__(config, cloud_name=\"chameleon\")\n\n    def create_reservation(self, name: str, node_type: str, count: int, start_date: str = None, end_date: str = None, duration: int = None) -&gt; bool:\n        \"\"\"\n        Creates a node reservation (lease) in Chameleon Cloud using python-chi.\n        \"\"\"\n        cloud_config = self.get_cloud_config(\"chameleon\")\n        site = cloud_config.get(\"site\", \"CHI@TACC\")\n        project = cloud_config.get(\"project_name\")\n\n        if not project:\n            print(\"Error: 'project_name' must be configured in clouds.yaml for Chameleon reservations.\")\n            return False\n\n        try:\n            # Configure chi\n            chi.use_site(site)\n            chi.set(\"project_name\", project)\n\n            reservations = []\n            chi.lease.add_node_reservation(\n                reservations,\n                node_type=node_type,\n                count=count,\n            )\n\n            # Determine start and end dates\n            if duration:\n                s, e = chi.lease.lease_duration(days=duration)\n            elif start_date and end_date:\n                s, e = start_date, end_date\n            else:\n                s, e = chi.lease.lease_duration(days=1)\n\n            chi.lease.create_lease(\n                name,\n                reservations,\n                start_date=s,\n                end_date=e,\n            )\n            return True\n        except Exception as e:\n            print(f\"Error creating reservation in Chameleon: {e}\")\n            return False\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.ChameleonProvider-functions","title":"Methods:","text":""},{"location":"api.html#cloudmesh.ai.command.vm.ChameleonProvider.create_reservation","title":"create_reservation","text":"<pre><code>create_reservation(name, node_type, count, start_date=None, end_date=None, duration=None)\n</code></pre> <p>Creates a node reservation (lease) in Chameleon Cloud using python-chi.</p> Source code in <code>src/cloudmesh/ai/vm/openstack/ChameleonManager.py</code> <pre><code>def create_reservation(self, name: str, node_type: str, count: int, start_date: str = None, end_date: str = None, duration: int = None) -&gt; bool:\n    \"\"\"\n    Creates a node reservation (lease) in Chameleon Cloud using python-chi.\n    \"\"\"\n    cloud_config = self.get_cloud_config(\"chameleon\")\n    site = cloud_config.get(\"site\", \"CHI@TACC\")\n    project = cloud_config.get(\"project_name\")\n\n    if not project:\n        print(\"Error: 'project_name' must be configured in clouds.yaml for Chameleon reservations.\")\n        return False\n\n    try:\n        # Configure chi\n        chi.use_site(site)\n        chi.set(\"project_name\", project)\n\n        reservations = []\n        chi.lease.add_node_reservation(\n            reservations,\n            node_type=node_type,\n            count=count,\n        )\n\n        # Determine start and end dates\n        if duration:\n            s, e = chi.lease.lease_duration(days=duration)\n        elif start_date and end_date:\n            s, e = start_date, end_date\n        else:\n            s, e = chi.lease.lease_duration(days=1)\n\n        chi.lease.create_lease(\n            name,\n            reservations,\n            start_date=s,\n            end_date=e,\n        )\n        return True\n    except Exception as e:\n        print(f\"Error creating reservation in Chameleon: {e}\")\n        return False\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.CloudMeshError","title":"CloudMeshError","text":"<p>               Bases: <code>Exception</code></p> <p>Base exception for all cloudmesh-ai-vm errors.</p> Source code in <code>src/cloudmesh/ai/vm/exceptions.py</code> <pre><code>class CloudMeshError(Exception):\n    \"\"\"Base exception for all cloudmesh-ai-vm errors.\"\"\"\n    pass\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.GlobalConfig","title":"GlobalConfig  <code>dataclass</code>","text":"Source code in <code>src/cloudmesh/ai/vm/config_models.py</code> <pre><code>@dataclass\nclass GlobalConfig:\n    username: str\n    counter: int\n    default_cloud: str\n    clouds: Dict[str, ProviderConfig] = field(default_factory=dict)\n    last_vm: Optional[str] = None\n\n    @classmethod\n    def from_dict(cls, data: Dict[str, Any]):\n        clouds_data = data.get(\"clouds\", {})\n        clouds = {name: ProviderConfig.from_dict(name, cfg) for name, cfg in clouds_data.items()}\n        return cls(\n            username=data.get(\"username\", \"user\"),\n            counter=data.get(\"counter\", 0),\n            default_cloud=data.get(\"default_cloud\", \"\"),\n            clouds=clouds,\n            last_vm=data.get(\"last_vm\")\n        )\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.GoogleProvider","title":"GoogleProvider","text":"<p>               Bases: <code>LibcloudManager</code></p> <p>Google Compute Engine implementation of the LibcloudManager.</p> Source code in <code>src/cloudmesh/ai/vm/google/GoogleManager.py</code> <pre><code>class Provider(LibcloudManager):\n    \"\"\"\n    Google Compute Engine implementation of the LibcloudManager.\n    \"\"\"\n\n    def __init__(self, config):\n        super().__init__(config, cloud_name=\"google\")\n\n    def _get_driver(self):\n        cloud_config = self.get_cloud_config(\"google\")\n        return GCEDriver(\n            project_id=cloud_config.get(\"project_id\"),\n            private_key=cloud_config.get(\"private_key\")\n        )\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.JetstreamProvider","title":"JetstreamProvider","text":"<p>               Bases: <code>OpenstackManager</code></p> <p>Jetstream implementation of the OpenstackManager.</p> Source code in <code>src/cloudmesh/ai/vm/openstack/JetstreamManager.py</code> <pre><code>class Provider(OpenstackManager):\n    \"\"\"\n    Jetstream implementation of the OpenstackManager.\n    \"\"\"\n\n    def __init__(self, config):\n        super().__init__(config, cloud_name=\"jetstream\")\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.LimaProvider","title":"LimaProvider","text":"<p>               Bases: <code>CloudBaseManager</code></p> <p>Lima implementation of the CloudBaseManager. Uses the 'limactl' CLI tool to manage local VMs on macOS.</p> Source code in <code>src/cloudmesh/ai/vm/local/LimaManager.py</code> <pre><code>class Provider(CloudBaseManager):\n    \"\"\"\n    Lima implementation of the CloudBaseManager.\n    Uses the 'limactl' CLI tool to manage local VMs on macOS.\n    \"\"\"\n\n    def __init__(self, config):\n        super().__init__(config)\n        self._verify_installation()\n\n    def _verify_installation(self):\n        \"\"\"Ensure limactl is installed on the system.\"\"\"\n        try:\n            subprocess.run([\"limactl\", \"--version\"], capture_output=True, check=True)\n        except (subprocess.CalledProcessError, FileNotFoundError):\n            raise ProviderError(\"limactl not found. Please install Lima (brew install lima) to use this provider.\")\n\n    def _run_command(self, command: List[str]) -&gt; subprocess.CompletedProcess:\n        \"\"\"Helper to run shell commands and stream output in real-time.\"\"\"\n        try:\n            process = subprocess.Popen(\n                command,\n                stdout=subprocess.PIPE,\n                stderr=subprocess.STDOUT,\n                text=True,\n                bufsize=1\n            )\n\n            full_output = []\n            for line in process.stdout:\n                print(line, end=\"\")\n                full_output.append(line)\n\n            process.wait()\n\n            if process.returncode != 0:\n                raise subprocess.CalledProcessError(\n                    process.returncode, \n                    command, \n                    output=\"\".join(full_output),\n                    stderr=\"\".join(full_output)\n                )\n\n            return subprocess.CompletedProcess(\n                args=command, \n                returncode=process.returncode, \n                stdout=\"\".join(full_output), \n                stderr=None\n            )\n        except subprocess.CalledProcessError as e:\n            raise e\n        except Exception as e:\n            print(f\"Unexpected error executing command {' '.join(command)}: {e}\")\n            raise e\n\n\n    def _run_command_silent(self, command: List[str]) -&gt; subprocess.CompletedProcess:\n        \"\"\"Helper to run shell commands silently and return the result.\"\"\"\n        return subprocess.run(\n            command,\n            capture_output=True,\n            text=True,\n            check=True\n        )\n\n    def start(self, name: Optional[str] = None) -&gt; str:\n        \"\"\"\n        Starts (launches) a Lima VM. Supports built-in templates or custom YAML paths.\n        \"\"\"\n        cloud_config = self.get_cloud_config(\"lima\")\n        template = cloud_config.get(\"template\", \"ubuntu\")\n\n        # Name sanitization (underscores to hyphens)\n        vm_name = name\n        if not vm_name:\n            vm_name = \"lima-vm\"\n        vm_name = vm_name.replace(\"_\", \"-\")\n\n        # Resolve template path if it's a file\n        expanded_template = os.path.expanduser(template)\n        if os.path.exists(expanded_template):\n            template_arg = expanded_template\n        else:\n            # For built-in templates, Lima expects 'template:name' when using --name\n            template_arg = f\"template:{template}\"\n\n        command = [\"limactl\", \"start\", \"--name\", vm_name, template_arg]\n\n        self._run_command(command)\n        return vm_name\n\n    def stop(self, name: Optional[str] = None) -&gt; bool:\n        \"\"\"Stops a Lima VM.\"\"\"\n        if not name:\n            print(\"Error: VM name is required to stop.\")\n            return False\n\n        try:\n            self._run_command([\"limactl\", \"stop\", name])\n            return True\n        except subprocess.CalledProcessError:\n            return False\n\n    def delete(self, name: Optional[str] = None) -&gt; bool:\n        \"\"\"Deletes a Lima VM.\"\"\"\n        if not name:\n            print(\"Error: VM name is required to delete.\")\n            return False\n\n        try:\n            # limactl delete usually requires confirmation, use -f for force\n            self._run_command([\"limactl\", \"delete\", \"-f\", name])\n            return True\n        except subprocess.CalledProcessError:\n            return False\n\n    def list(self) -&gt; List[Dict[str, Any]]:\n        \"\"\"Lists Lima VMs.\"\"\"\n        try:\n            result = self._run_command([\"limactl\", \"list\"])\n            lines = result.stdout.strip().split(\"\\n\")\n            if len(lines) &lt; 2:\n                return []\n\n            # Parse headers\n            headers = lines[0].split()\n            vms = []\n\n            for line in lines[1:]:\n                parts = line.split()\n                if len(parts) &gt;= 2:\n                    vm_info = {headers[i]: parts[i] for i in range(min(len(headers), len(parts)))}\n\n                    # Normalize keys for ssh_config and other tools\n                    # Lima uses NAME and SSH\n                    name = vm_info.get(\"NAME\") or vm_info.get(\"Name\")\n                    ip = vm_info.get(\"SSH\") or vm_info.get(\"IP\")\n\n                    vm_info[\"Name\"] = name\n                    vm_info[\"IP\"] = ip\n\n                    vms.append(vm_info)\n\n            return vms\n\n        except subprocess.CalledProcessError:\n            return []\n\n    def info(self, name: str) -&gt; Dict[str, Any]:\n        \"\"\"\n        Gets detailed information about a Lima VM.\n        \"\"\"\n        vms = self.list()\n        for vm in vms:\n            if vm.get(\"Name\") == name:\n                return vm\n        return {\"error\": f\"VM {name} not found\"}\n\n    def run_command(self, name: str, cmd: str) -&gt; str:\n        \"\"\"\n        Executes a command on a Lima VM.\n        \"\"\"\n        try:\n            # limactl shell &lt;name&gt; &lt;cmd&gt;\n            # We use sh -c to ensure the command is executed correctly\n            result = self._run_command([\"limactl\", \"shell\", name, \"sh\", \"-c\", cmd])\n            return result.stdout.strip()\n        except subprocess.CalledProcessError as e:\n            return f\"Error executing command: {e.stderr or e.output}\"\n        except Exception as e:\n            return f\"Unexpected error: {e}\"\n\n    def login(self, name: Optional[str] = None) -&gt; bool:\n        \"\"\"Logs into a Lima VM using 'limactl shell'.\"\"\"\n        if not name:\n            print(\"Error: VM name is required to login.\")\n            return False\n\n        try:\n            # Use subprocess.run for interactive shell\n            subprocess.run([\"limactl\", \"shell\", name], check=True)\n            return True\n        except subprocess.CalledProcessError:\n            return False\n\n    def suspend(self, name: Optional[str] = None) -&gt; bool:\n        \"\"\"Lima doesn't have a native 'suspend' in the same way; using 'stop'.\"\"\"\n        return self.stop(name)\n\n    def restart(self, name: Optional[str] = None) -&gt; bool:\n        \"\"\"Restarts a Lima VM.\"\"\"\n        if not name:\n            print(\"Error: VM name is required to restart.\")\n            return False\n\n        try:\n            self.stop(name)\n            self._run_command([\"limactl\", \"start\", name])\n            return True\n        except subprocess.CalledProcessError:\n            return False\n\n    def get_images(self) -&gt; List[Dict[str, Any]]:\n        \"\"\"\n        Lists available images in Lima using 'limactl start --list-templates'.\n        \"\"\"\n        try:\n            # Use silent command to avoid leaking raw output to console\n            result = self._run_command_silent([\"limactl\", \"start\", \"--list-templates\"])\n            lines = result.stdout.strip().split(\"\\n\")\n            if not lines:\n                return []\n\n            images = []\n            # Look for lines starting with '- ' which typically indicate templates\n            for line in lines:\n                line = line.strip()\n                if line.startswith(\"- \"):\n                    # Example line: \"- ubuntu (Ubuntu 22.04 LTS)\"\n                    content = line[2:].strip()\n                    if content:\n                        template_name = content.split()[0]\n                        images.append({\"name\": template_name})\n\n            return images\n        except (subprocess.CalledProcessError, Exception) as e:\n            print(f\"Error listing Lima images: {e}\")\n            return []\n\n    def get_flavors(self) -&gt; List[Dict[str, Any]]:\n        \"\"\"Lima uses templates rather than flavors.\"\"\"\n        return None\n\n    def get_keys(self) -&gt; List[Dict[str, Any]]:\n        \"\"\"Lima manages SSH keys automatically.\"\"\"\n        return [{\"name\": \"lima-ssh-key\", \"path\": \"~/.ssh/id_rsa\"}]\n\n    def get_security_groups(self) -&gt; List[Dict[str, Any]]:\n        \"\"\"Lima uses port forwarding instead of security groups.\"\"\"\n        return [{\"name\": \"default\", \"description\": \"Local port forwarding\"}]\n\n\n    def check_requirements(self) -&gt; bool:\n        \"\"\"\n        Checks if the requirements for this provider are met on the current system.\n        \"\"\"\n        import shutil\n        import platform\n        if platform.system() not in [\"Darwin\", \"Linux\"]:\n            return False\n        return shutil.which(\"limactl\") is not None\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.LimaProvider-functions","title":"Methods:","text":""},{"location":"api.html#cloudmesh.ai.command.vm.LimaProvider.check_requirements","title":"check_requirements","text":"<pre><code>check_requirements()\n</code></pre> <p>Checks if the requirements for this provider are met on the current system.</p> Source code in <code>src/cloudmesh/ai/vm/local/LimaManager.py</code> <pre><code>def check_requirements(self) -&gt; bool:\n    \"\"\"\n    Checks if the requirements for this provider are met on the current system.\n    \"\"\"\n    import shutil\n    import platform\n    if platform.system() not in [\"Darwin\", \"Linux\"]:\n        return False\n    return shutil.which(\"limactl\") is not None\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.LimaProvider.delete","title":"delete","text":"<pre><code>delete(name=None)\n</code></pre> <p>Deletes a Lima VM.</p> Source code in <code>src/cloudmesh/ai/vm/local/LimaManager.py</code> <pre><code>def delete(self, name: Optional[str] = None) -&gt; bool:\n    \"\"\"Deletes a Lima VM.\"\"\"\n    if not name:\n        print(\"Error: VM name is required to delete.\")\n        return False\n\n    try:\n        # limactl delete usually requires confirmation, use -f for force\n        self._run_command([\"limactl\", \"delete\", \"-f\", name])\n        return True\n    except subprocess.CalledProcessError:\n        return False\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.LimaProvider.get_flavors","title":"get_flavors","text":"<pre><code>get_flavors()\n</code></pre> <p>Lima uses templates rather than flavors.</p> Source code in <code>src/cloudmesh/ai/vm/local/LimaManager.py</code> <pre><code>def get_flavors(self) -&gt; List[Dict[str, Any]]:\n    \"\"\"Lima uses templates rather than flavors.\"\"\"\n    return None\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.LimaProvider.get_images","title":"get_images","text":"<pre><code>get_images()\n</code></pre> <p>Lists available images in Lima using 'limactl start --list-templates'.</p> Source code in <code>src/cloudmesh/ai/vm/local/LimaManager.py</code> <pre><code>def get_images(self) -&gt; List[Dict[str, Any]]:\n    \"\"\"\n    Lists available images in Lima using 'limactl start --list-templates'.\n    \"\"\"\n    try:\n        # Use silent command to avoid leaking raw output to console\n        result = self._run_command_silent([\"limactl\", \"start\", \"--list-templates\"])\n        lines = result.stdout.strip().split(\"\\n\")\n        if not lines:\n            return []\n\n        images = []\n        # Look for lines starting with '- ' which typically indicate templates\n        for line in lines:\n            line = line.strip()\n            if line.startswith(\"- \"):\n                # Example line: \"- ubuntu (Ubuntu 22.04 LTS)\"\n                content = line[2:].strip()\n                if content:\n                    template_name = content.split()[0]\n                    images.append({\"name\": template_name})\n\n        return images\n    except (subprocess.CalledProcessError, Exception) as e:\n        print(f\"Error listing Lima images: {e}\")\n        return []\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.LimaProvider.get_keys","title":"get_keys","text":"<pre><code>get_keys()\n</code></pre> <p>Lima manages SSH keys automatically.</p> Source code in <code>src/cloudmesh/ai/vm/local/LimaManager.py</code> <pre><code>def get_keys(self) -&gt; List[Dict[str, Any]]:\n    \"\"\"Lima manages SSH keys automatically.\"\"\"\n    return [{\"name\": \"lima-ssh-key\", \"path\": \"~/.ssh/id_rsa\"}]\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.LimaProvider.get_security_groups","title":"get_security_groups","text":"<pre><code>get_security_groups()\n</code></pre> <p>Lima uses port forwarding instead of security groups.</p> Source code in <code>src/cloudmesh/ai/vm/local/LimaManager.py</code> <pre><code>def get_security_groups(self) -&gt; List[Dict[str, Any]]:\n    \"\"\"Lima uses port forwarding instead of security groups.\"\"\"\n    return [{\"name\": \"default\", \"description\": \"Local port forwarding\"}]\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.LimaProvider.info","title":"info","text":"<pre><code>info(name)\n</code></pre> <p>Gets detailed information about a Lima VM.</p> Source code in <code>src/cloudmesh/ai/vm/local/LimaManager.py</code> <pre><code>def info(self, name: str) -&gt; Dict[str, Any]:\n    \"\"\"\n    Gets detailed information about a Lima VM.\n    \"\"\"\n    vms = self.list()\n    for vm in vms:\n        if vm.get(\"Name\") == name:\n            return vm\n    return {\"error\": f\"VM {name} not found\"}\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.LimaProvider.list","title":"list","text":"<pre><code>list()\n</code></pre> <p>Lists Lima VMs.</p> Source code in <code>src/cloudmesh/ai/vm/local/LimaManager.py</code> <pre><code>def list(self) -&gt; List[Dict[str, Any]]:\n    \"\"\"Lists Lima VMs.\"\"\"\n    try:\n        result = self._run_command([\"limactl\", \"list\"])\n        lines = result.stdout.strip().split(\"\\n\")\n        if len(lines) &lt; 2:\n            return []\n\n        # Parse headers\n        headers = lines[0].split()\n        vms = []\n\n        for line in lines[1:]:\n            parts = line.split()\n            if len(parts) &gt;= 2:\n                vm_info = {headers[i]: parts[i] for i in range(min(len(headers), len(parts)))}\n\n                # Normalize keys for ssh_config and other tools\n                # Lima uses NAME and SSH\n                name = vm_info.get(\"NAME\") or vm_info.get(\"Name\")\n                ip = vm_info.get(\"SSH\") or vm_info.get(\"IP\")\n\n                vm_info[\"Name\"] = name\n                vm_info[\"IP\"] = ip\n\n                vms.append(vm_info)\n\n        return vms\n\n    except subprocess.CalledProcessError:\n        return []\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.LimaProvider.login","title":"login","text":"<pre><code>login(name=None)\n</code></pre> <p>Logs into a Lima VM using 'limactl shell'.</p> Source code in <code>src/cloudmesh/ai/vm/local/LimaManager.py</code> <pre><code>def login(self, name: Optional[str] = None) -&gt; bool:\n    \"\"\"Logs into a Lima VM using 'limactl shell'.\"\"\"\n    if not name:\n        print(\"Error: VM name is required to login.\")\n        return False\n\n    try:\n        # Use subprocess.run for interactive shell\n        subprocess.run([\"limactl\", \"shell\", name], check=True)\n        return True\n    except subprocess.CalledProcessError:\n        return False\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.LimaProvider.restart","title":"restart","text":"<pre><code>restart(name=None)\n</code></pre> <p>Restarts a Lima VM.</p> Source code in <code>src/cloudmesh/ai/vm/local/LimaManager.py</code> <pre><code>def restart(self, name: Optional[str] = None) -&gt; bool:\n    \"\"\"Restarts a Lima VM.\"\"\"\n    if not name:\n        print(\"Error: VM name is required to restart.\")\n        return False\n\n    try:\n        self.stop(name)\n        self._run_command([\"limactl\", \"start\", name])\n        return True\n    except subprocess.CalledProcessError:\n        return False\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.LimaProvider.run_command","title":"run_command","text":"<pre><code>run_command(name, cmd)\n</code></pre> <p>Executes a command on a Lima VM.</p> Source code in <code>src/cloudmesh/ai/vm/local/LimaManager.py</code> <pre><code>def run_command(self, name: str, cmd: str) -&gt; str:\n    \"\"\"\n    Executes a command on a Lima VM.\n    \"\"\"\n    try:\n        # limactl shell &lt;name&gt; &lt;cmd&gt;\n        # We use sh -c to ensure the command is executed correctly\n        result = self._run_command([\"limactl\", \"shell\", name, \"sh\", \"-c\", cmd])\n        return result.stdout.strip()\n    except subprocess.CalledProcessError as e:\n        return f\"Error executing command: {e.stderr or e.output}\"\n    except Exception as e:\n        return f\"Unexpected error: {e}\"\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.LimaProvider.start","title":"start","text":"<pre><code>start(name=None)\n</code></pre> <p>Starts (launches) a Lima VM. Supports built-in templates or custom YAML paths.</p> Source code in <code>src/cloudmesh/ai/vm/local/LimaManager.py</code> <pre><code>def start(self, name: Optional[str] = None) -&gt; str:\n    \"\"\"\n    Starts (launches) a Lima VM. Supports built-in templates or custom YAML paths.\n    \"\"\"\n    cloud_config = self.get_cloud_config(\"lima\")\n    template = cloud_config.get(\"template\", \"ubuntu\")\n\n    # Name sanitization (underscores to hyphens)\n    vm_name = name\n    if not vm_name:\n        vm_name = \"lima-vm\"\n    vm_name = vm_name.replace(\"_\", \"-\")\n\n    # Resolve template path if it's a file\n    expanded_template = os.path.expanduser(template)\n    if os.path.exists(expanded_template):\n        template_arg = expanded_template\n    else:\n        # For built-in templates, Lima expects 'template:name' when using --name\n        template_arg = f\"template:{template}\"\n\n    command = [\"limactl\", \"start\", \"--name\", vm_name, template_arg]\n\n    self._run_command(command)\n    return vm_name\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.LimaProvider.stop","title":"stop","text":"<pre><code>stop(name=None)\n</code></pre> <p>Stops a Lima VM.</p> Source code in <code>src/cloudmesh/ai/vm/local/LimaManager.py</code> <pre><code>def stop(self, name: Optional[str] = None) -&gt; bool:\n    \"\"\"Stops a Lima VM.\"\"\"\n    if not name:\n        print(\"Error: VM name is required to stop.\")\n        return False\n\n    try:\n        self._run_command([\"limactl\", \"stop\", name])\n        return True\n    except subprocess.CalledProcessError:\n        return False\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.LimaProvider.suspend","title":"suspend","text":"<pre><code>suspend(name=None)\n</code></pre> <p>Lima doesn't have a native 'suspend' in the same way; using 'stop'.</p> Source code in <code>src/cloudmesh/ai/vm/local/LimaManager.py</code> <pre><code>def suspend(self, name: Optional[str] = None) -&gt; bool:\n    \"\"\"Lima doesn't have a native 'suspend' in the same way; using 'stop'.\"\"\"\n    return self.stop(name)\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.MultipassProvider","title":"MultipassProvider","text":"<p>               Bases: <code>CloudBaseManager</code></p> <p>Multipass implementation of the CloudBaseManager. Uses the 'multipass' CLI tool to manage local VMs.</p> Source code in <code>src/cloudmesh/ai/vm/local/MultipassManager.py</code> <pre><code>class Provider(CloudBaseManager):\n    \"\"\"\n    Multipass implementation of the CloudBaseManager.\n    Uses the 'multipass' CLI tool to manage local VMs.\n    \"\"\"\n\n    def _run_command(self, command: List[str]) -&gt; subprocess.CompletedProcess:\n        \"\"\"Helper to run shell commands and stream output in real-time.\"\"\"\n        try:\n            # Use Popen to stream output line by line\n            process = subprocess.Popen(\n                command,\n                stdout=subprocess.PIPE,\n                stderr=subprocess.STDOUT,\n                text=True,\n                bufsize=1\n            )\n\n            full_output = []\n            for line in process.stdout:\n                print(line, end=\"\")\n                full_output.append(line)\n\n            process.wait()\n\n            if process.returncode != 0:\n                # Create a CalledProcessError to maintain compatibility with existing error handling\n                raise subprocess.CalledProcessError(\n                    process.returncode, \n                    command, \n                    output=\"\".join(full_output),\n                    stderr=\"\".join(full_output)\n                )\n\n            return subprocess.CompletedProcess(\n                args=command, \n                returncode=process.returncode, \n                stdout=\"\".join(full_output), \n                stderr=None\n            )\n        except subprocess.CalledProcessError as e:\n            # The error is already printed via the loop above, but we keep the exception for the caller\n            raise e\n        except Exception as e:\n            print(f\"Unexpected error executing command {' '.join(command)}: {e}\")\n            raise e\n\n    def start(self, name: Optional[str] = None) -&gt; str:\n        \"\"\"\n        Starts (launches) a Multipass VM with optional resource configurations.\n        \"\"\"\n        cloud_config = self.get_cloud_config(\"multipass\")\n        image = cloud_config.get(\"image\", \"22.04\")\n        cpus = cloud_config.get(\"cpus\")\n        memory = cloud_config.get(\"memory\")\n        disk = cloud_config.get(\"disk\")\n\n        command = [\"multipass\", \"launch\"]\n\n        if cpus:\n            command.extend([\"-c\", str(cpus)])\n        if memory:\n            command.extend([\"-m\", str(memory)])\n        if disk:\n            command.extend([\"-d\", str(disk)])\n\n        if name:\n            command.extend([\"-n\", name])\n\n        command.append(str(image))\n\n        self._run_command(command)\n\n        return name if name else \"multipass-generated\"\n\n\n    def stop(self, name: Optional[str] = None) -&gt; bool:\n        \"\"\"\n        Stops a Multipass VM.\n        \"\"\"\n        if not name:\n            print(\"Error: VM name is required to stop.\")\n            return False\n\n        try:\n            self._run_command([\"multipass\", \"stop\", name])\n            return True\n        except subprocess.CalledProcessError:\n            return False\n\n    def delete(self, name: Optional[str] = None) -&gt; bool:\n        \"\"\"\n        Deletes a Multipass VM.\n        \"\"\"\n        if not name:\n            print(\"Error: VM name is required to delete.\")\n            return False\n\n        try:\n            # Multipass requires a delete then a purge\n            self._run_command([\"multipass\", \"delete\", name])\n            self._run_command([\"multipass\", \"purge\"])\n            return True\n        except subprocess.CalledProcessError:\n            return False\n\n    def list(self) -&gt; List[Dict[str, Any]]:\n        \"\"\"\n        Lists Multipass VMs.\n        \"\"\"\n        try:\n            result = self._run_command([\"multipass\", \"list\"])\n            lines = result.stdout.strip().split(\"\\n\")\n            if len(lines) &lt; 2:\n                return []\n\n            # Parse headers\n            headers = lines[0].split()\n            vms = []\n\n            for line in lines[1:]:\n                parts = line.split()\n                if len(parts) &gt;= 2:\n                    vm_info = {headers[i]: parts[i] for i in range(min(len(headers), len(parts)))}\n                    vms.append(vm_info)\n\n            return vms\n        except subprocess.CalledProcessError:\n            return []\n\n    def login(self, name: Optional[str] = None) -&gt; bool:\n        \"\"\"\n        Logs into a Multipass VM using 'multipass shell'.\n        \"\"\"\n        if not name:\n            print(\"Error: VM name is required to login.\")\n            return False\n\n        try:\n            # 'shell' is interactive, so we use subprocess.run without capture_output\n            # to let the user interact with the VM.\n            subprocess.run([\"multipass\", \"shell\", name], check=True)\n            return True\n        except subprocess.CalledProcessError:\n            return False\n\n    def suspend(self, name: Optional[str] = None) -&gt; bool:\n        \"\"\"\n        Multipass does not have a native 'suspend'. Implementing as 'stop'.\n        \"\"\"\n        return self.stop(name)\n\n    def restart(self, name: Optional[str] = None) -&gt; bool:\n        \"\"\"\n        Restarts a Multipass VM.\n        \"\"\"\n        if not name:\n            print(\"Error: VM name is required to restart.\")\n            return False\n\n        try:\n            self.stop(name)\n            self._run_command([\"multipass\", \"start\", name])\n            return True\n        except subprocess.CalledProcessError:\n            return False\n\n\n    def run_command(self, name: str, cmd: str) -&gt; str:\n        \"\"\"\n        Executes a command on a Multipass VM.\n        \"\"\"\n        try:\n            # multipass exec &lt;name&gt; &lt;cmd&gt;\n            result = self._run_command([\"multipass\", \"exec\", name, \"sh\", \"-c\", cmd])\n            return result.stdout.strip()\n        except subprocess.CalledProcessError as e:\n            return f\"Error executing command: {e.stderr or e.output}\"\n        except Exception as e:\n            return f\"Unexpected error: {e}\"\n\n\n\n    def info(self, name: str) -&gt; Dict[str, Any]:\n        \"\"\"\n        Gets detailed information about a Multipass VM.\n        \"\"\"\n        try:\n            result = self._run_command([\"multipass\", \"info\", name])\n            lines = result.stdout.strip().split(\"\\n\")\n            info = {}\n            for line in lines:\n                if \":\" in line:\n                    key, value = line.split(\":\", 1)\n                    info[key.strip()] = value.strip()\n            return info\n        except subprocess.CalledProcessError:\n            return {\"error\": f\"Could not get info for VM {name}\"}\n\n    def get_flavors(self) -&gt; List[Dict[str, Any]]:\n        \"\"\"\n        Multipass uses CPU/RAM/Disk options rather than fixed flavors.\n        Returning a list of common custom options.\n        \"\"\"\n        return [\n            {\"name\": \"default\", \"cpu\": 1, \"ram\": \"1GiB\", \"disk\": \"5GiB\"},\n            {\"name\": \"medium\", \"cpu\": 2, \"ram\": \"2GiB\", \"disk\": \"10GiB\"},\n            {\"name\": \"large\", \"cpu\": 4, \"ram\": \"4GiB\", \"disk\": \"20GiB\"},\n        ]\n\n\n    def _run_command_silent(self, command: List[str]) -&gt; subprocess.CompletedProcess:\n        \"\"\"Helper to run shell commands silently and return the result.\"\"\"\n        return subprocess.run(\n            command,\n            capture_output=True,\n            text=True,\n            check=True\n        )\n\n    def get_images(self) -&gt; List[Dict[str, Any]]:\n        \"\"\"\n        Lists available images in Multipass using 'multipass find'.\n        \"\"\"\n        try:\n            # Use silent command to avoid leaking raw output to console\n            result = self._run_command_silent([\"multipass\", \"find\"])\n            lines = result.stdout.strip().split(\"\\\\n\")\n            if not lines:\n                return []\n\n            images = []\n            # Skip the header line \"Available images:\" and parse lines starting with \"- \"\n            for line in lines:\n                line = line.strip()\n                if line.startswith(\"- \"):\n                    # Example line: \"- 22.04 (Ubuntu Jammy Jellyfish)\"\n                    # Extract the version string (e.g., 22.04)\n                    content = line[2:].strip()\n                    if content:\n                        image_name = content.split()[0]\n                        images.append({\"name\": image_name})\n\n            return images\n        except (subprocess.CalledProcessError, Exception) as e:\n            print(f\"Error listing Multipass images: {e}\")\n            return []\n\n    def check_requirements(self) -&gt; bool:\n        \"\"\"\n        Checks if the requirements for this provider are met on the current system.\n        \"\"\"\n        import shutil\n        return shutil.which(\"multipass\") is not None                \n\n    def get_keys(self) -&gt; List[Dict[str, Any]]:\n        \"\"\"\n        Multipass manages its own keys internally.\n        \"\"\"\n        return [{\"name\": \"multipass-default-key\", \"path\": \"~/.ssh/multipass_rsa\"}]\n\n    def get_security_groups(self) -&gt; List[Dict[str, Any]]:\n        \"\"\"\n        Multipass does not use security groups.\n        \"\"\"\n        return [{\"name\": \"default\", \"description\": \"Local network access\"}]\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.MultipassProvider-functions","title":"Methods:","text":""},{"location":"api.html#cloudmesh.ai.command.vm.MultipassProvider.check_requirements","title":"check_requirements","text":"<pre><code>check_requirements()\n</code></pre> <p>Checks if the requirements for this provider are met on the current system.</p> Source code in <code>src/cloudmesh/ai/vm/local/MultipassManager.py</code> <pre><code>def check_requirements(self) -&gt; bool:\n    \"\"\"\n    Checks if the requirements for this provider are met on the current system.\n    \"\"\"\n    import shutil\n    return shutil.which(\"multipass\") is not None                \n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.MultipassProvider.delete","title":"delete","text":"<pre><code>delete(name=None)\n</code></pre> <p>Deletes a Multipass VM.</p> Source code in <code>src/cloudmesh/ai/vm/local/MultipassManager.py</code> <pre><code>def delete(self, name: Optional[str] = None) -&gt; bool:\n    \"\"\"\n    Deletes a Multipass VM.\n    \"\"\"\n    if not name:\n        print(\"Error: VM name is required to delete.\")\n        return False\n\n    try:\n        # Multipass requires a delete then a purge\n        self._run_command([\"multipass\", \"delete\", name])\n        self._run_command([\"multipass\", \"purge\"])\n        return True\n    except subprocess.CalledProcessError:\n        return False\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.MultipassProvider.get_flavors","title":"get_flavors","text":"<pre><code>get_flavors()\n</code></pre> <p>Multipass uses CPU/RAM/Disk options rather than fixed flavors. Returning a list of common custom options.</p> Source code in <code>src/cloudmesh/ai/vm/local/MultipassManager.py</code> <pre><code>def get_flavors(self) -&gt; List[Dict[str, Any]]:\n    \"\"\"\n    Multipass uses CPU/RAM/Disk options rather than fixed flavors.\n    Returning a list of common custom options.\n    \"\"\"\n    return [\n        {\"name\": \"default\", \"cpu\": 1, \"ram\": \"1GiB\", \"disk\": \"5GiB\"},\n        {\"name\": \"medium\", \"cpu\": 2, \"ram\": \"2GiB\", \"disk\": \"10GiB\"},\n        {\"name\": \"large\", \"cpu\": 4, \"ram\": \"4GiB\", \"disk\": \"20GiB\"},\n    ]\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.MultipassProvider.get_images","title":"get_images","text":"<pre><code>get_images()\n</code></pre> <p>Lists available images in Multipass using 'multipass find'.</p> Source code in <code>src/cloudmesh/ai/vm/local/MultipassManager.py</code> <pre><code>def get_images(self) -&gt; List[Dict[str, Any]]:\n    \"\"\"\n    Lists available images in Multipass using 'multipass find'.\n    \"\"\"\n    try:\n        # Use silent command to avoid leaking raw output to console\n        result = self._run_command_silent([\"multipass\", \"find\"])\n        lines = result.stdout.strip().split(\"\\\\n\")\n        if not lines:\n            return []\n\n        images = []\n        # Skip the header line \"Available images:\" and parse lines starting with \"- \"\n        for line in lines:\n            line = line.strip()\n            if line.startswith(\"- \"):\n                # Example line: \"- 22.04 (Ubuntu Jammy Jellyfish)\"\n                # Extract the version string (e.g., 22.04)\n                content = line[2:].strip()\n                if content:\n                    image_name = content.split()[0]\n                    images.append({\"name\": image_name})\n\n        return images\n    except (subprocess.CalledProcessError, Exception) as e:\n        print(f\"Error listing Multipass images: {e}\")\n        return []\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.MultipassProvider.get_keys","title":"get_keys","text":"<pre><code>get_keys()\n</code></pre> <p>Multipass manages its own keys internally.</p> Source code in <code>src/cloudmesh/ai/vm/local/MultipassManager.py</code> <pre><code>def get_keys(self) -&gt; List[Dict[str, Any]]:\n    \"\"\"\n    Multipass manages its own keys internally.\n    \"\"\"\n    return [{\"name\": \"multipass-default-key\", \"path\": \"~/.ssh/multipass_rsa\"}]\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.MultipassProvider.get_security_groups","title":"get_security_groups","text":"<pre><code>get_security_groups()\n</code></pre> <p>Multipass does not use security groups.</p> Source code in <code>src/cloudmesh/ai/vm/local/MultipassManager.py</code> <pre><code>def get_security_groups(self) -&gt; List[Dict[str, Any]]:\n    \"\"\"\n    Multipass does not use security groups.\n    \"\"\"\n    return [{\"name\": \"default\", \"description\": \"Local network access\"}]\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.MultipassProvider.info","title":"info","text":"<pre><code>info(name)\n</code></pre> <p>Gets detailed information about a Multipass VM.</p> Source code in <code>src/cloudmesh/ai/vm/local/MultipassManager.py</code> <pre><code>def info(self, name: str) -&gt; Dict[str, Any]:\n    \"\"\"\n    Gets detailed information about a Multipass VM.\n    \"\"\"\n    try:\n        result = self._run_command([\"multipass\", \"info\", name])\n        lines = result.stdout.strip().split(\"\\n\")\n        info = {}\n        for line in lines:\n            if \":\" in line:\n                key, value = line.split(\":\", 1)\n                info[key.strip()] = value.strip()\n        return info\n    except subprocess.CalledProcessError:\n        return {\"error\": f\"Could not get info for VM {name}\"}\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.MultipassProvider.list","title":"list","text":"<pre><code>list()\n</code></pre> <p>Lists Multipass VMs.</p> Source code in <code>src/cloudmesh/ai/vm/local/MultipassManager.py</code> <pre><code>def list(self) -&gt; List[Dict[str, Any]]:\n    \"\"\"\n    Lists Multipass VMs.\n    \"\"\"\n    try:\n        result = self._run_command([\"multipass\", \"list\"])\n        lines = result.stdout.strip().split(\"\\n\")\n        if len(lines) &lt; 2:\n            return []\n\n        # Parse headers\n        headers = lines[0].split()\n        vms = []\n\n        for line in lines[1:]:\n            parts = line.split()\n            if len(parts) &gt;= 2:\n                vm_info = {headers[i]: parts[i] for i in range(min(len(headers), len(parts)))}\n                vms.append(vm_info)\n\n        return vms\n    except subprocess.CalledProcessError:\n        return []\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.MultipassProvider.login","title":"login","text":"<pre><code>login(name=None)\n</code></pre> <p>Logs into a Multipass VM using 'multipass shell'.</p> Source code in <code>src/cloudmesh/ai/vm/local/MultipassManager.py</code> <pre><code>def login(self, name: Optional[str] = None) -&gt; bool:\n    \"\"\"\n    Logs into a Multipass VM using 'multipass shell'.\n    \"\"\"\n    if not name:\n        print(\"Error: VM name is required to login.\")\n        return False\n\n    try:\n        # 'shell' is interactive, so we use subprocess.run without capture_output\n        # to let the user interact with the VM.\n        subprocess.run([\"multipass\", \"shell\", name], check=True)\n        return True\n    except subprocess.CalledProcessError:\n        return False\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.MultipassProvider.restart","title":"restart","text":"<pre><code>restart(name=None)\n</code></pre> <p>Restarts a Multipass VM.</p> Source code in <code>src/cloudmesh/ai/vm/local/MultipassManager.py</code> <pre><code>def restart(self, name: Optional[str] = None) -&gt; bool:\n    \"\"\"\n    Restarts a Multipass VM.\n    \"\"\"\n    if not name:\n        print(\"Error: VM name is required to restart.\")\n        return False\n\n    try:\n        self.stop(name)\n        self._run_command([\"multipass\", \"start\", name])\n        return True\n    except subprocess.CalledProcessError:\n        return False\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.MultipassProvider.run_command","title":"run_command","text":"<pre><code>run_command(name, cmd)\n</code></pre> <p>Executes a command on a Multipass VM.</p> Source code in <code>src/cloudmesh/ai/vm/local/MultipassManager.py</code> <pre><code>def run_command(self, name: str, cmd: str) -&gt; str:\n    \"\"\"\n    Executes a command on a Multipass VM.\n    \"\"\"\n    try:\n        # multipass exec &lt;name&gt; &lt;cmd&gt;\n        result = self._run_command([\"multipass\", \"exec\", name, \"sh\", \"-c\", cmd])\n        return result.stdout.strip()\n    except subprocess.CalledProcessError as e:\n        return f\"Error executing command: {e.stderr or e.output}\"\n    except Exception as e:\n        return f\"Unexpected error: {e}\"\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.MultipassProvider.start","title":"start","text":"<pre><code>start(name=None)\n</code></pre> <p>Starts (launches) a Multipass VM with optional resource configurations.</p> Source code in <code>src/cloudmesh/ai/vm/local/MultipassManager.py</code> <pre><code>def start(self, name: Optional[str] = None) -&gt; str:\n    \"\"\"\n    Starts (launches) a Multipass VM with optional resource configurations.\n    \"\"\"\n    cloud_config = self.get_cloud_config(\"multipass\")\n    image = cloud_config.get(\"image\", \"22.04\")\n    cpus = cloud_config.get(\"cpus\")\n    memory = cloud_config.get(\"memory\")\n    disk = cloud_config.get(\"disk\")\n\n    command = [\"multipass\", \"launch\"]\n\n    if cpus:\n        command.extend([\"-c\", str(cpus)])\n    if memory:\n        command.extend([\"-m\", str(memory)])\n    if disk:\n        command.extend([\"-d\", str(disk)])\n\n    if name:\n        command.extend([\"-n\", name])\n\n    command.append(str(image))\n\n    self._run_command(command)\n\n    return name if name else \"multipass-generated\"\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.MultipassProvider.stop","title":"stop","text":"<pre><code>stop(name=None)\n</code></pre> <p>Stops a Multipass VM.</p> Source code in <code>src/cloudmesh/ai/vm/local/MultipassManager.py</code> <pre><code>def stop(self, name: Optional[str] = None) -&gt; bool:\n    \"\"\"\n    Stops a Multipass VM.\n    \"\"\"\n    if not name:\n        print(\"Error: VM name is required to stop.\")\n        return False\n\n    try:\n        self._run_command([\"multipass\", \"stop\", name])\n        return True\n    except subprocess.CalledProcessError:\n        return False\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.MultipassProvider.suspend","title":"suspend","text":"<pre><code>suspend(name=None)\n</code></pre> <p>Multipass does not have a native 'suspend'. Implementing as 'stop'.</p> Source code in <code>src/cloudmesh/ai/vm/local/MultipassManager.py</code> <pre><code>def suspend(self, name: Optional[str] = None) -&gt; bool:\n    \"\"\"\n    Multipass does not have a native 'suspend'. Implementing as 'stop'.\n    \"\"\"\n    return self.stop(name)\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.StateManager","title":"StateManager","text":"<p>Handles persistent state for cloudmesh-ai-vm, including VM counters and the last used VM.</p> Source code in <code>src/cloudmesh/ai/vm/state_manager.py</code> <pre><code>class StateManager:\n    \"\"\"\n    Handles persistent state for cloudmesh-ai-vm, including VM counters\n    and the last used VM.\n    \"\"\"\n    def __init__(self, config_path: str):\n        self.config_path = config_path\n        self.config: Optional[GlobalConfig] = self._load_config()\n\n    def _load_config(self) -&gt; GlobalConfig:\n        if not os.path.exists(self.config_path):\n            logger.warning(f\"Config file not found at {self.config_path}. Using defaults.\")\n            return GlobalConfig(username=\"user\", counter=0, default_cloud=\"\")\n\n        try:\n            with open(self.config_path, 'r') as f:\n                data = yaml.safe_load(f) or {}\n                return GlobalConfig.from_dict(data)\n        except Exception as e:\n            logger.error(f\"Error loading config file: {e}\")\n            return GlobalConfig(username=\"user\", counter=0, default_cloud=\"\")\n\n    def save(self):\n        \"\"\"Saves the current state back to the YAML file.\"\"\"\n        try:\n            # Convert dataclass to dict for yaml dump\n            data = {\n                \"username\": self.config.username,\n                \"counter\": self.config.counter,\n                \"default_cloud\": self.config.default_cloud,\n                \"last_vm\": self.config.last_vm,\n                \"clouds\": {name: cfg.__dict__ for name, cfg in self.config.clouds.items()}\n            }\n            # Remove None values to keep yaml clean\n            def clean_nones(d):\n                if not isinstance(d, dict): return d\n                return {k: clean_nones(v) for k, v in d.items() if v is not None}\n\n            with open(self.config_path, 'w') as f:\n                yaml.dump(clean_nones(data), f, default_flow_style=False)\n        except Exception as e:\n            logger.error(f\"Error saving config file: {e}\")\n\n    def increment_counter(self) -&gt; int:\n        self.config.counter += 1\n        self.save()\n        return self.config.counter\n\n    def set_last_vm(self, vm_name: str):\n        self.config.last_vm = vm_name\n        self.save()\n\n    def get_last_vm(self) -&gt; Optional[str]:\n        return self.config.last_vm\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.StateManager-functions","title":"Methods:","text":""},{"location":"api.html#cloudmesh.ai.command.vm.StateManager.save","title":"save","text":"<pre><code>save()\n</code></pre> <p>Saves the current state back to the YAML file.</p> Source code in <code>src/cloudmesh/ai/vm/state_manager.py</code> <pre><code>def save(self):\n    \"\"\"Saves the current state back to the YAML file.\"\"\"\n    try:\n        # Convert dataclass to dict for yaml dump\n        data = {\n            \"username\": self.config.username,\n            \"counter\": self.config.counter,\n            \"default_cloud\": self.config.default_cloud,\n            \"last_vm\": self.config.last_vm,\n            \"clouds\": {name: cfg.__dict__ for name, cfg in self.config.clouds.items()}\n        }\n        # Remove None values to keep yaml clean\n        def clean_nones(d):\n            if not isinstance(d, dict): return d\n            return {k: clean_nones(v) for k, v in d.items() if v is not None}\n\n        with open(self.config_path, 'w') as f:\n            yaml.dump(clean_nones(data), f, default_flow_style=False)\n    except Exception as e:\n        logger.error(f\"Error saving config file: {e}\")\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.VBoxProvider","title":"VBoxProvider","text":"<p>               Bases: <code>CloudBaseManager</code></p> <p>VirtualBox implementation of the CloudBaseManager. Uses the 'VBoxManage' CLI tool to manage local VMs.</p> Source code in <code>src/cloudmesh/ai/vm/local/VBoxManager.py</code> <pre><code>class Provider(CloudBaseManager):\n    \"\"\"\n    VirtualBox implementation of the CloudBaseManager.\n    Uses the 'VBoxManage' CLI tool to manage local VMs.\n    \"\"\"\n\n    def _run_command(self, command: List[str]) -&gt; subprocess.CompletedProcess:\n        \"\"\"Helper to run shell commands.\"\"\"\n        try:\n            return subprocess.run(command, capture_output=True, text=True, check=True)\n        except subprocess.CalledProcessError as e:\n            print(f\"Error executing command {' '.join(command)}: {e.stderr}\")\n            raise e\n\n    def start(self, name: Optional[str] = None) -&gt; str:\n        \"\"\"\n        Starts a VirtualBox VM.\n        Note: VBoxManage does not have a simple 'launch' like Multipass.\n        This implementation assumes the VM already exists.\n        \"\"\"\n        if not name:\n            print(\"Error: VirtualBox requires a specific VM name to start.\")\n            return \"error\"\n\n        try:\n            # Start the VM in headless mode (no GUI window)\n            self._run_command([\"VBoxManage\", \"startvm\", name, \"--type\", \"headless\"])\n            return name\n        except subprocess.CalledProcessError:\n            return \"error\"\n\n    def stop(self, name: Optional[str] = None) -&gt; bool:\n        \"\"\"\n        Stops a VirtualBox VM.\n        \"\"\"\n        if not name:\n            print(\"Error: VM name is required to stop.\")\n            return False\n\n        try:\n            # poweroff is the fastest way to stop. acpishutdown is cleaner but slower.\n            self._run_command([\"VBoxManage\", \"controlvm\", name, \"poweroff\"])\n            return True\n        except subprocess.CalledProcessError:\n            return False\n\n    def delete(self, name: Optional[str] = None) -&gt; bool:\n        \"\"\"\n        Deletes a VirtualBox VM and its registered files.\n        \"\"\"\n        if not name:\n            print(\"Error: VM name is required to delete.\")\n            return False\n\n        try:\n            # unregistervm --delete removes the VM from the list and deletes the files on disk\n            self._run_command([\"VBoxManage\", \"unregistervm\", name, \"--delete\"])\n            return True\n        except subprocess.CalledProcessError:\n            return False\n\n    def list(self) -&gt; List[Dict[str, Any]]:\n        \"\"\"\n        Lists VirtualBox VMs.\n        \"\"\"\n        try:\n            result = self._run_command([\"VBoxManage\", \"list\", \"vms\"])\n            lines = result.stdout.strip().split(\"\\n\")\n            if not lines or lines == ['']:\n                return []\n\n            vms = []\n            for line in lines:\n                # VBoxManage list vms output format: \"VM Name\" {uuid}\n                match = re.match(r'\"([^\"]+)\"\\s+\\{([^}]+)\\}', line)\n                if match:\n                    vms.append({\n                        \"Name\": match.group(1),\n                        \"UUID\": match.group(2)\n                    })\n            return vms\n        except subprocess.CalledProcessError:\n            return []\n\n    def login(self, name: Optional[str] = None) -&gt; bool:\n        \"\"\"\n        VirtualBox does not provide a built-in shell like 'multipass shell'.\n        This method informs the user to use SSH or the GUI.\n        \"\"\"\n        if not name:\n            print(\"Error: VM name is required to login.\")\n            return False\n\n        print(f\"Note: VirtualBox does not have a native shell. Please use SSH to log into {name}.\")\n        print(f\"Example: ssh username@{ip_address}\")\n        return False\n\n    def suspend(self, name: Optional[str] = None) -&gt; bool:\n        \"\"\"\n        Suspends a VirtualBox VM (Saves state).\n        \"\"\"\n        if not name:\n            print(\"Error: VM name is required to suspend.\")\n            return False\n\n        try:\n            self._run_command([\"VBoxManage\", \"controlvm\", name, \"savestate\"])\n            return True\n        except subprocess.CalledProcessError:\n            return False\n\n    def restart(self, name: Optional[str] = None) -&gt; bool:\n        \"\"\"\n        Restarts a VirtualBox VM.\n        \"\"\"\n        if not name:\n            print(\"Error: VM name is required to restart.\")\n            return False\n\n        try:\n            self.stop(name)\n            self.start(name)\n            return True\n        except subprocess.CalledProcessError:\n            return False\n\n    def get_flavors(self) -&gt; List[Dict[str, Any]]:\n        \"\"\"\n        VirtualBox resources are configured per VM.\n        \"\"\"\n        return [{\"name\": \"default\", \"description\": \"Customizable in VBoxManage modifyvm\"}]\n\n    def get_keys(self) -&gt; List[Dict[str, Any]]:\n        \"\"\"\n        VirtualBox doesn't manage SSH keys.\n        \"\"\"\n        return [{\"name\": \"local-key\", \"path\": \"~/.ssh/id_rsa\"}]\n\n    def get_security_groups(self) -&gt; List[Dict[str, Any]]:\n        \"\"\"\n        VirtualBox uses Networking Modes (NAT, Bridged, etc.)\n        \"\"\"\n        return [{\"name\": \"NAT\", \"description\": \"Default VirtualBox NAT network\"}]\n\n\n    def check_requirements(self) -&gt; bool:\n        \"\"\"\n        Checks if the requirements for this provider are met on the current system.\n        \"\"\"\n        import shutil\n        return shutil.which(\"VBoxManage\") is not None\n\n    def run_command(self, name: str, cmd: str) -&gt; str:\n        \"\"\"\n        Executes a command on the VirtualBox VM using guestcontrol.\n        Requires Guest Additions.\n        \"\"\"\n        if not name:\n            raise ProviderError(\"VM name is required to run command.\")\n\n        cloud_config = self.get_cloud_config(\"vbox\")\n        username = cloud_config.get(\"username\", \"user\")\n        password = cloud_config.get(\"password\", \"\")\n\n        try:\n            # VBoxManage guestcontrol &lt;vmname&gt; run --username=&lt;user&gt; --password=&lt;pass&gt; --exe &lt;exe&gt; --args &lt;args&gt;\n            result = self._run_command([\n                \"VBoxManage\", \"guestcontrol\", name, \"run\",\n                f\"--username={username}\",\n                f\"--password={password}\",\n                \"--exe\", \"/bin/sh\",\n                \"--args\", \"-c\", cmd\n            ])\n            return result.stdout\n        except subprocess.CalledProcessError as e:\n            return f\"Error executing command (requires Guest Additions): {e.stderr}\"\n\n    def info(self, name: str) -&gt; Dict[str, Any]:\n        \"\"\"Gets detailed information about a VirtualBox VM.\"\"\"\n        try:\n            result = self._run_command([\"VBoxManage\", \"showvminfo\", name])\n            return {\"Name\": name, \"RawInfo\": result.stdout}\n        except Exception as e:\n            return {\"error\": str(e)}\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.VBoxProvider-functions","title":"Methods:","text":""},{"location":"api.html#cloudmesh.ai.command.vm.VBoxProvider.check_requirements","title":"check_requirements","text":"<pre><code>check_requirements()\n</code></pre> <p>Checks if the requirements for this provider are met on the current system.</p> Source code in <code>src/cloudmesh/ai/vm/local/VBoxManager.py</code> <pre><code>def check_requirements(self) -&gt; bool:\n    \"\"\"\n    Checks if the requirements for this provider are met on the current system.\n    \"\"\"\n    import shutil\n    return shutil.which(\"VBoxManage\") is not None\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.VBoxProvider.delete","title":"delete","text":"<pre><code>delete(name=None)\n</code></pre> <p>Deletes a VirtualBox VM and its registered files.</p> Source code in <code>src/cloudmesh/ai/vm/local/VBoxManager.py</code> <pre><code>def delete(self, name: Optional[str] = None) -&gt; bool:\n    \"\"\"\n    Deletes a VirtualBox VM and its registered files.\n    \"\"\"\n    if not name:\n        print(\"Error: VM name is required to delete.\")\n        return False\n\n    try:\n        # unregistervm --delete removes the VM from the list and deletes the files on disk\n        self._run_command([\"VBoxManage\", \"unregistervm\", name, \"--delete\"])\n        return True\n    except subprocess.CalledProcessError:\n        return False\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.VBoxProvider.get_flavors","title":"get_flavors","text":"<pre><code>get_flavors()\n</code></pre> <p>VirtualBox resources are configured per VM.</p> Source code in <code>src/cloudmesh/ai/vm/local/VBoxManager.py</code> <pre><code>def get_flavors(self) -&gt; List[Dict[str, Any]]:\n    \"\"\"\n    VirtualBox resources are configured per VM.\n    \"\"\"\n    return [{\"name\": \"default\", \"description\": \"Customizable in VBoxManage modifyvm\"}]\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.VBoxProvider.get_keys","title":"get_keys","text":"<pre><code>get_keys()\n</code></pre> <p>VirtualBox doesn't manage SSH keys.</p> Source code in <code>src/cloudmesh/ai/vm/local/VBoxManager.py</code> <pre><code>def get_keys(self) -&gt; List[Dict[str, Any]]:\n    \"\"\"\n    VirtualBox doesn't manage SSH keys.\n    \"\"\"\n    return [{\"name\": \"local-key\", \"path\": \"~/.ssh/id_rsa\"}]\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.VBoxProvider.get_security_groups","title":"get_security_groups","text":"<pre><code>get_security_groups()\n</code></pre> <p>VirtualBox uses Networking Modes (NAT, Bridged, etc.)</p> Source code in <code>src/cloudmesh/ai/vm/local/VBoxManager.py</code> <pre><code>def get_security_groups(self) -&gt; List[Dict[str, Any]]:\n    \"\"\"\n    VirtualBox uses Networking Modes (NAT, Bridged, etc.)\n    \"\"\"\n    return [{\"name\": \"NAT\", \"description\": \"Default VirtualBox NAT network\"}]\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.VBoxProvider.info","title":"info","text":"<pre><code>info(name)\n</code></pre> <p>Gets detailed information about a VirtualBox VM.</p> Source code in <code>src/cloudmesh/ai/vm/local/VBoxManager.py</code> <pre><code>def info(self, name: str) -&gt; Dict[str, Any]:\n    \"\"\"Gets detailed information about a VirtualBox VM.\"\"\"\n    try:\n        result = self._run_command([\"VBoxManage\", \"showvminfo\", name])\n        return {\"Name\": name, \"RawInfo\": result.stdout}\n    except Exception as e:\n        return {\"error\": str(e)}\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.VBoxProvider.list","title":"list","text":"<pre><code>list()\n</code></pre> <p>Lists VirtualBox VMs.</p> Source code in <code>src/cloudmesh/ai/vm/local/VBoxManager.py</code> <pre><code>def list(self) -&gt; List[Dict[str, Any]]:\n    \"\"\"\n    Lists VirtualBox VMs.\n    \"\"\"\n    try:\n        result = self._run_command([\"VBoxManage\", \"list\", \"vms\"])\n        lines = result.stdout.strip().split(\"\\n\")\n        if not lines or lines == ['']:\n            return []\n\n        vms = []\n        for line in lines:\n            # VBoxManage list vms output format: \"VM Name\" {uuid}\n            match = re.match(r'\"([^\"]+)\"\\s+\\{([^}]+)\\}', line)\n            if match:\n                vms.append({\n                    \"Name\": match.group(1),\n                    \"UUID\": match.group(2)\n                })\n        return vms\n    except subprocess.CalledProcessError:\n        return []\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.VBoxProvider.login","title":"login","text":"<pre><code>login(name=None)\n</code></pre> <p>VirtualBox does not provide a built-in shell like 'multipass shell'. This method informs the user to use SSH or the GUI.</p> Source code in <code>src/cloudmesh/ai/vm/local/VBoxManager.py</code> <pre><code>def login(self, name: Optional[str] = None) -&gt; bool:\n    \"\"\"\n    VirtualBox does not provide a built-in shell like 'multipass shell'.\n    This method informs the user to use SSH or the GUI.\n    \"\"\"\n    if not name:\n        print(\"Error: VM name is required to login.\")\n        return False\n\n    print(f\"Note: VirtualBox does not have a native shell. Please use SSH to log into {name}.\")\n    print(f\"Example: ssh username@{ip_address}\")\n    return False\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.VBoxProvider.restart","title":"restart","text":"<pre><code>restart(name=None)\n</code></pre> <p>Restarts a VirtualBox VM.</p> Source code in <code>src/cloudmesh/ai/vm/local/VBoxManager.py</code> <pre><code>def restart(self, name: Optional[str] = None) -&gt; bool:\n    \"\"\"\n    Restarts a VirtualBox VM.\n    \"\"\"\n    if not name:\n        print(\"Error: VM name is required to restart.\")\n        return False\n\n    try:\n        self.stop(name)\n        self.start(name)\n        return True\n    except subprocess.CalledProcessError:\n        return False\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.VBoxProvider.run_command","title":"run_command","text":"<pre><code>run_command(name, cmd)\n</code></pre> <p>Executes a command on the VirtualBox VM using guestcontrol. Requires Guest Additions.</p> Source code in <code>src/cloudmesh/ai/vm/local/VBoxManager.py</code> <pre><code>def run_command(self, name: str, cmd: str) -&gt; str:\n    \"\"\"\n    Executes a command on the VirtualBox VM using guestcontrol.\n    Requires Guest Additions.\n    \"\"\"\n    if not name:\n        raise ProviderError(\"VM name is required to run command.\")\n\n    cloud_config = self.get_cloud_config(\"vbox\")\n    username = cloud_config.get(\"username\", \"user\")\n    password = cloud_config.get(\"password\", \"\")\n\n    try:\n        # VBoxManage guestcontrol &lt;vmname&gt; run --username=&lt;user&gt; --password=&lt;pass&gt; --exe &lt;exe&gt; --args &lt;args&gt;\n        result = self._run_command([\n            \"VBoxManage\", \"guestcontrol\", name, \"run\",\n            f\"--username={username}\",\n            f\"--password={password}\",\n            \"--exe\", \"/bin/sh\",\n            \"--args\", \"-c\", cmd\n        ])\n        return result.stdout\n    except subprocess.CalledProcessError as e:\n        return f\"Error executing command (requires Guest Additions): {e.stderr}\"\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.VBoxProvider.start","title":"start","text":"<pre><code>start(name=None)\n</code></pre> <p>Starts a VirtualBox VM. Note: VBoxManage does not have a simple 'launch' like Multipass. This implementation assumes the VM already exists.</p> Source code in <code>src/cloudmesh/ai/vm/local/VBoxManager.py</code> <pre><code>def start(self, name: Optional[str] = None) -&gt; str:\n    \"\"\"\n    Starts a VirtualBox VM.\n    Note: VBoxManage does not have a simple 'launch' like Multipass.\n    This implementation assumes the VM already exists.\n    \"\"\"\n    if not name:\n        print(\"Error: VirtualBox requires a specific VM name to start.\")\n        return \"error\"\n\n    try:\n        # Start the VM in headless mode (no GUI window)\n        self._run_command([\"VBoxManage\", \"startvm\", name, \"--type\", \"headless\"])\n        return name\n    except subprocess.CalledProcessError:\n        return \"error\"\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.VBoxProvider.stop","title":"stop","text":"<pre><code>stop(name=None)\n</code></pre> <p>Stops a VirtualBox VM.</p> Source code in <code>src/cloudmesh/ai/vm/local/VBoxManager.py</code> <pre><code>def stop(self, name: Optional[str] = None) -&gt; bool:\n    \"\"\"\n    Stops a VirtualBox VM.\n    \"\"\"\n    if not name:\n        print(\"Error: VM name is required to stop.\")\n        return False\n\n    try:\n        # poweroff is the fastest way to stop. acpishutdown is cleaner but slower.\n        self._run_command([\"VBoxManage\", \"controlvm\", name, \"poweroff\"])\n        return True\n    except subprocess.CalledProcessError:\n        return False\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.VBoxProvider.suspend","title":"suspend","text":"<pre><code>suspend(name=None)\n</code></pre> <p>Suspends a VirtualBox VM (Saves state).</p> Source code in <code>src/cloudmesh/ai/vm/local/VBoxManager.py</code> <pre><code>def suspend(self, name: Optional[str] = None) -&gt; bool:\n    \"\"\"\n    Suspends a VirtualBox VM (Saves state).\n    \"\"\"\n    if not name:\n        print(\"Error: VM name is required to suspend.\")\n        return False\n\n    try:\n        self._run_command([\"VBoxManage\", \"controlvm\", name, \"savestate\"])\n        return True\n    except subprocess.CalledProcessError:\n        return False\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.VMContext","title":"VMContext","text":"<p>Context object to share state and config across CLI commands.</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>class VMContext:\n    \"\"\"Context object to share state and config across CLI commands.\"\"\"\n    def __init__(self):\n        self.cloud_override: Optional[str] = None\n        self.provider = None\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.Wsl2Provider","title":"Wsl2Provider","text":"<p>               Bases: <code>CloudBaseManager</code></p> <p>WSL2 implementation of the CloudBaseManager. Uses the 'wsl.exe' CLI tool to manage Linux distributions.</p> Source code in <code>src/cloudmesh/ai/vm/local/Wsl2Manager.py</code> <pre><code>class Provider(CloudBaseManager):\n    \"\"\"\n    WSL2 implementation of the CloudBaseManager.\n    Uses the 'wsl.exe' CLI tool to manage Linux distributions.\n    \"\"\"\n\n    def _run_command(self, command: List[str]) -&gt; subprocess.CompletedProcess:\n        \"\"\"Helper to run shell commands.\"\"\"\n        try:\n            return subprocess.run(command, capture_output=True, text=True, check=True)\n        except subprocess.CalledProcessError as e:\n            print(f\"Error executing command {' '.join(command)}: {e.stderr}\")\n            raise e\n\n    def start(self, name: Optional[str] = None) -&gt; str:\n        \"\"\"\n        Starts a WSL2 distribution. \n        If the distribution doesn't exist, it attempts to import it from a rootfs image.\n        \"\"\"\n        if not name:\n            print(\"Error: WSL2 requires a specific distribution name to start.\")\n            return \"error\"\n\n        # Check if distro already exists\n        try:\n            list_output = self._run_command([\"wsl\", \"--list\"]).stdout\n            if name in list_output:\n                # Distro exists, just start it\n                self._run_command([\"wsl\", \"-d\", name])\n                return name\n        except subprocess.CalledProcessError:\n            pass\n\n        # Attempt to import if not exists\n        cloud_config = self.get_cloud_config(\"wsl2\")\n        rootfs = cloud_config.get(\"rootfs\")\n        install_dir = cloud_config.get(\"install_dir\", \"C:\\\\WSL\")\n\n        if not rootfs:\n            print(f\"Error: Rootfs image path not configured in YAML for wsl2. Cannot import {name}.\")\n            return \"error\"\n\n        try:\n            # wsl --import &lt;Distro&gt; &lt;InstallLocation&gt; &lt;FileName&gt;\n            self._run_command([\"wsl\", \"--import\", name, install_dir, rootfs])\n            self._run_command([\"wsl\", \"-d\", name])\n            return name\n        except subprocess.CalledProcessError:\n            return \"error\"\n\n    def stop(self, name: Optional[str] = None) -&gt; bool:\n        \"\"\"\n        Stops (terminates) a WSL2 distribution.\n        \"\"\"\n        if not name:\n            print(\"Error: Distro name is required to stop.\")\n            return False\n\n        try:\n            self._run_command([\"wsl\", \"--terminate\", name])\n            return True\n        except subprocess.CalledProcessError:\n            return False\n\n    def delete(self, name: Optional[str] = None) -&gt; bool:\n        \"\"\"\n        Deletes (unregisters) a WSL2 distribution.\n        \"\"\"\n        if not name:\n            print(\"Error: Distro name is required to delete.\")\n            return False\n\n        try:\n            self._run_command([\"wsl\", \"--unregister\", name])\n            return True\n        except subprocess.CalledProcessError:\n            return False\n\n    def list(self) -&gt; List[Dict[str, Any]]:\n        \"\"\"\n        Lists WSL2 distributions.\n        \"\"\"\n        try:\n            result = self._run_command([\"wsl\", \"--list\", \"--verbose\"])\n            lines = result.stdout.strip().split(\"\\n\")\n            if len(lines) &lt; 2:\n                return []\n\n            # WSL list output usually has headers: NAME STATE VERSION\n            # It can be a bit messy with whitespace, so we use regex or split\n            vms = []\n            for line in lines[1:]:\n                # Extract name, state, version\n                # Example: Ubuntu  Running  2\n                parts = line.split()\n                if len(parts) &gt;= 2:\n                    vms.append({\n                        \"Name\": parts[0],\n                        \"State\": parts[1],\n                        \"Version\": parts[2] if len(parts) &gt; 2 else \"Unknown\"\n                    })\n            return vms\n        except subprocess.CalledProcessError:\n            return []\n\n    def login(self, name: Optional[str] = None) -&gt; bool:\n        \"\"\"\n        Logs into a WSL2 distribution.\n        \"\"\"\n        if not name:\n            print(\"Error: Distro name is required to login.\")\n            return False\n\n        try:\n            # launch interactive shell\n            subprocess.run([\"wsl\", \"-d\", name], check=True)\n            return True\n        except subprocess.CalledProcessError:\n            return False\n\n    def suspend(self, name: Optional[str] = None) -&gt; bool:\n        \"\"\"\n        WSL2 doesn't have a 'suspend' mode; we use terminate.\n        \"\"\"\n        return self.stop(name)\n\n    def restart(self, name: Optional[str] = None) -&gt; bool:\n        \"\"\"\n        Restarts a WSL2 distribution.\n        \"\"\"\n        if not name:\n            print(\"Error: Distro name is required to restart.\")\n            return False\n\n        try:\n            self.stop(name)\n            self.start(name)\n            return True\n        except subprocess.CalledProcessError:\n            return False\n\n    def get_flavors(self) -&gt; List[Dict[str, Any]]:\n        \"\"\"\n        WSL2 does not have 'flavors' in the cloud sense.\n        \"\"\"\n        return [{\"name\": \"standard\", \"description\": \"Default WSL2 Resource allocation\"}]\n\n    def get_keys(self) -&gt; List[Dict[str, Any]]:\n        \"\"\"\n        WSL2 uses local user keys.\n        \"\"\"\n        return [{\"name\": \"local-user-key\", \"path\": \"~/.ssh/id_rsa\"}]\n\n    def get_security_groups(self) -&gt; List[Dict[str, Any]]:\n        \"\"\"\n        WSL2 uses host firewall/networking.\n        \"\"\"\n        return [{\"name\": \"default\", \"description\": \"Host network access\"}]\n\n    def link_ssh_dir(self, name: Optional[str] = None) -&gt; bool:\n        \"\"\"\n        Creates a symbolic link from the WSL2 home .ssh directory \n        to the host OS's .ssh directory.\n\n        Expected config:\n        - wsl_username: username inside WSL2\n        - host_username: username on the Windows host\n        \"\"\"\n        if not name:\n            print(\"Error: Distro name is required to link SSH directory.\")\n            return False\n\n        cloud_config = self.get_cloud_config(\"wsl2\")\n        wsl_user = cloud_config.get(\"wsl_username\")\n        host_user = cloud_config.get(\"host_username\")\n\n        if not wsl_user or not host_user:\n            print(\"Error: 'wsl_username' and 'host_username' must be configured in clouds.yaml to link SSH directory.\")\n            return False\n\n        # Path on host: C:\\Users\\&lt;host_user&gt;\\.ssh -&gt; /mnt/c/Users/&lt;host_user&gt;/.ssh\n        host_ssh_path = f\"/mnt/c/Users/{host_user}/.ssh\"\n        # Path in WSL2: /home/&lt;wsl_user&gt;/.ssh\n        wsl_ssh_path = f\"/home/{wsl_user}/.ssh\"\n\n        try:\n            # We use -u root to ensure we have permissions to modify the home directory\n            # 1. Remove existing .ssh dir/link if it exists\n            # 2. Create the symbolic link\n            shell_command = f\"rm -rf {wsl_ssh_path} &amp;&amp; ln -s {host_ssh_path} {wsl_ssh_path}\"\n            self._run_command([\"wsl\", \"-d\", name, \"-u\", \"root\", \"sh\", \"-c\", shell_command])\n            return True\n        except subprocess.CalledProcessError:\n            return False\n\n\n    def run_command(self, name: str, cmd: str) -&gt; str:\n        \"\"\"\n        Executes a command on the WSL2 distribution.\n        \"\"\"\n        if not name:\n            raise ProviderError(\"VM name is required to run command.\")\n\n        cloud_config = self.get_cloud_config(\"wsl2\")\n        wsl_user = cloud_config.get(\"wsl_username\", \"root\")\n\n        try:\n            # Use wsl -d &lt;name&gt; -u &lt;user&gt; sh -c &lt;command&gt;\n            result = self._run_command([\"wsl\", \"-d\", name, \"-u\", wsl_user, \"sh\", \"-c\", cmd])\n            return result.stdout\n        except subprocess.CalledProcessError as e:\n            return f\"Error executing command: {e.stderr}\"\n\n    def info(self, name: str) -&gt; Dict[str, Any]:\n        \"\"\"Gets detailed information about a WSL2 distribution.\"\"\"\n        try:\n            result = self._run_command([\"wsl\", \"--list\", \"--verbose\"])\n            for line in result.stdout.splitlines():\n                if name in line:\n                    return {\"RawInfo\": line.strip()}\n            return {\"error\": f\"Distribution {name} not found\"}\n        except Exception as e:\n            return {\"error\": str(e)}\n\n    def check_requirements(self) -&gt; bool:\n        \"\"\"\n        Checks if the requirements for this provider are met on the current system.\n        \"\"\"\n        import shutil\n        import platform\n        if platform.system() != \"Windows\":\n            return False\n        return shutil.which(\"wsl\") is not None\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.Wsl2Provider-functions","title":"Methods:","text":""},{"location":"api.html#cloudmesh.ai.command.vm.Wsl2Provider.check_requirements","title":"check_requirements","text":"<pre><code>check_requirements()\n</code></pre> <p>Checks if the requirements for this provider are met on the current system.</p> Source code in <code>src/cloudmesh/ai/vm/local/Wsl2Manager.py</code> <pre><code>def check_requirements(self) -&gt; bool:\n    \"\"\"\n    Checks if the requirements for this provider are met on the current system.\n    \"\"\"\n    import shutil\n    import platform\n    if platform.system() != \"Windows\":\n        return False\n    return shutil.which(\"wsl\") is not None\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.Wsl2Provider.delete","title":"delete","text":"<pre><code>delete(name=None)\n</code></pre> <p>Deletes (unregisters) a WSL2 distribution.</p> Source code in <code>src/cloudmesh/ai/vm/local/Wsl2Manager.py</code> <pre><code>def delete(self, name: Optional[str] = None) -&gt; bool:\n    \"\"\"\n    Deletes (unregisters) a WSL2 distribution.\n    \"\"\"\n    if not name:\n        print(\"Error: Distro name is required to delete.\")\n        return False\n\n    try:\n        self._run_command([\"wsl\", \"--unregister\", name])\n        return True\n    except subprocess.CalledProcessError:\n        return False\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.Wsl2Provider.get_flavors","title":"get_flavors","text":"<pre><code>get_flavors()\n</code></pre> <p>WSL2 does not have 'flavors' in the cloud sense.</p> Source code in <code>src/cloudmesh/ai/vm/local/Wsl2Manager.py</code> <pre><code>def get_flavors(self) -&gt; List[Dict[str, Any]]:\n    \"\"\"\n    WSL2 does not have 'flavors' in the cloud sense.\n    \"\"\"\n    return [{\"name\": \"standard\", \"description\": \"Default WSL2 Resource allocation\"}]\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.Wsl2Provider.get_keys","title":"get_keys","text":"<pre><code>get_keys()\n</code></pre> <p>WSL2 uses local user keys.</p> Source code in <code>src/cloudmesh/ai/vm/local/Wsl2Manager.py</code> <pre><code>def get_keys(self) -&gt; List[Dict[str, Any]]:\n    \"\"\"\n    WSL2 uses local user keys.\n    \"\"\"\n    return [{\"name\": \"local-user-key\", \"path\": \"~/.ssh/id_rsa\"}]\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.Wsl2Provider.get_security_groups","title":"get_security_groups","text":"<pre><code>get_security_groups()\n</code></pre> <p>WSL2 uses host firewall/networking.</p> Source code in <code>src/cloudmesh/ai/vm/local/Wsl2Manager.py</code> <pre><code>def get_security_groups(self) -&gt; List[Dict[str, Any]]:\n    \"\"\"\n    WSL2 uses host firewall/networking.\n    \"\"\"\n    return [{\"name\": \"default\", \"description\": \"Host network access\"}]\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.Wsl2Provider.info","title":"info","text":"<pre><code>info(name)\n</code></pre> <p>Gets detailed information about a WSL2 distribution.</p> Source code in <code>src/cloudmesh/ai/vm/local/Wsl2Manager.py</code> <pre><code>def info(self, name: str) -&gt; Dict[str, Any]:\n    \"\"\"Gets detailed information about a WSL2 distribution.\"\"\"\n    try:\n        result = self._run_command([\"wsl\", \"--list\", \"--verbose\"])\n        for line in result.stdout.splitlines():\n            if name in line:\n                return {\"RawInfo\": line.strip()}\n        return {\"error\": f\"Distribution {name} not found\"}\n    except Exception as e:\n        return {\"error\": str(e)}\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.Wsl2Provider.link_ssh_dir","title":"link_ssh_dir","text":"<pre><code>link_ssh_dir(name=None)\n</code></pre> <p>Creates a symbolic link from the WSL2 home .ssh directory  to the host OS's .ssh directory.</p> <p>Expected config: - wsl_username: username inside WSL2 - host_username: username on the Windows host</p> Source code in <code>src/cloudmesh/ai/vm/local/Wsl2Manager.py</code> <pre><code>def link_ssh_dir(self, name: Optional[str] = None) -&gt; bool:\n    \"\"\"\n    Creates a symbolic link from the WSL2 home .ssh directory \n    to the host OS's .ssh directory.\n\n    Expected config:\n    - wsl_username: username inside WSL2\n    - host_username: username on the Windows host\n    \"\"\"\n    if not name:\n        print(\"Error: Distro name is required to link SSH directory.\")\n        return False\n\n    cloud_config = self.get_cloud_config(\"wsl2\")\n    wsl_user = cloud_config.get(\"wsl_username\")\n    host_user = cloud_config.get(\"host_username\")\n\n    if not wsl_user or not host_user:\n        print(\"Error: 'wsl_username' and 'host_username' must be configured in clouds.yaml to link SSH directory.\")\n        return False\n\n    # Path on host: C:\\Users\\&lt;host_user&gt;\\.ssh -&gt; /mnt/c/Users/&lt;host_user&gt;/.ssh\n    host_ssh_path = f\"/mnt/c/Users/{host_user}/.ssh\"\n    # Path in WSL2: /home/&lt;wsl_user&gt;/.ssh\n    wsl_ssh_path = f\"/home/{wsl_user}/.ssh\"\n\n    try:\n        # We use -u root to ensure we have permissions to modify the home directory\n        # 1. Remove existing .ssh dir/link if it exists\n        # 2. Create the symbolic link\n        shell_command = f\"rm -rf {wsl_ssh_path} &amp;&amp; ln -s {host_ssh_path} {wsl_ssh_path}\"\n        self._run_command([\"wsl\", \"-d\", name, \"-u\", \"root\", \"sh\", \"-c\", shell_command])\n        return True\n    except subprocess.CalledProcessError:\n        return False\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.Wsl2Provider.list","title":"list","text":"<pre><code>list()\n</code></pre> <p>Lists WSL2 distributions.</p> Source code in <code>src/cloudmesh/ai/vm/local/Wsl2Manager.py</code> <pre><code>def list(self) -&gt; List[Dict[str, Any]]:\n    \"\"\"\n    Lists WSL2 distributions.\n    \"\"\"\n    try:\n        result = self._run_command([\"wsl\", \"--list\", \"--verbose\"])\n        lines = result.stdout.strip().split(\"\\n\")\n        if len(lines) &lt; 2:\n            return []\n\n        # WSL list output usually has headers: NAME STATE VERSION\n        # It can be a bit messy with whitespace, so we use regex or split\n        vms = []\n        for line in lines[1:]:\n            # Extract name, state, version\n            # Example: Ubuntu  Running  2\n            parts = line.split()\n            if len(parts) &gt;= 2:\n                vms.append({\n                    \"Name\": parts[0],\n                    \"State\": parts[1],\n                    \"Version\": parts[2] if len(parts) &gt; 2 else \"Unknown\"\n                })\n        return vms\n    except subprocess.CalledProcessError:\n        return []\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.Wsl2Provider.login","title":"login","text":"<pre><code>login(name=None)\n</code></pre> <p>Logs into a WSL2 distribution.</p> Source code in <code>src/cloudmesh/ai/vm/local/Wsl2Manager.py</code> <pre><code>def login(self, name: Optional[str] = None) -&gt; bool:\n    \"\"\"\n    Logs into a WSL2 distribution.\n    \"\"\"\n    if not name:\n        print(\"Error: Distro name is required to login.\")\n        return False\n\n    try:\n        # launch interactive shell\n        subprocess.run([\"wsl\", \"-d\", name], check=True)\n        return True\n    except subprocess.CalledProcessError:\n        return False\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.Wsl2Provider.restart","title":"restart","text":"<pre><code>restart(name=None)\n</code></pre> <p>Restarts a WSL2 distribution.</p> Source code in <code>src/cloudmesh/ai/vm/local/Wsl2Manager.py</code> <pre><code>def restart(self, name: Optional[str] = None) -&gt; bool:\n    \"\"\"\n    Restarts a WSL2 distribution.\n    \"\"\"\n    if not name:\n        print(\"Error: Distro name is required to restart.\")\n        return False\n\n    try:\n        self.stop(name)\n        self.start(name)\n        return True\n    except subprocess.CalledProcessError:\n        return False\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.Wsl2Provider.run_command","title":"run_command","text":"<pre><code>run_command(name, cmd)\n</code></pre> <p>Executes a command on the WSL2 distribution.</p> Source code in <code>src/cloudmesh/ai/vm/local/Wsl2Manager.py</code> <pre><code>def run_command(self, name: str, cmd: str) -&gt; str:\n    \"\"\"\n    Executes a command on the WSL2 distribution.\n    \"\"\"\n    if not name:\n        raise ProviderError(\"VM name is required to run command.\")\n\n    cloud_config = self.get_cloud_config(\"wsl2\")\n    wsl_user = cloud_config.get(\"wsl_username\", \"root\")\n\n    try:\n        # Use wsl -d &lt;name&gt; -u &lt;user&gt; sh -c &lt;command&gt;\n        result = self._run_command([\"wsl\", \"-d\", name, \"-u\", wsl_user, \"sh\", \"-c\", cmd])\n        return result.stdout\n    except subprocess.CalledProcessError as e:\n        return f\"Error executing command: {e.stderr}\"\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.Wsl2Provider.start","title":"start","text":"<pre><code>start(name=None)\n</code></pre> <p>Starts a WSL2 distribution.  If the distribution doesn't exist, it attempts to import it from a rootfs image.</p> Source code in <code>src/cloudmesh/ai/vm/local/Wsl2Manager.py</code> <pre><code>def start(self, name: Optional[str] = None) -&gt; str:\n    \"\"\"\n    Starts a WSL2 distribution. \n    If the distribution doesn't exist, it attempts to import it from a rootfs image.\n    \"\"\"\n    if not name:\n        print(\"Error: WSL2 requires a specific distribution name to start.\")\n        return \"error\"\n\n    # Check if distro already exists\n    try:\n        list_output = self._run_command([\"wsl\", \"--list\"]).stdout\n        if name in list_output:\n            # Distro exists, just start it\n            self._run_command([\"wsl\", \"-d\", name])\n            return name\n    except subprocess.CalledProcessError:\n        pass\n\n    # Attempt to import if not exists\n    cloud_config = self.get_cloud_config(\"wsl2\")\n    rootfs = cloud_config.get(\"rootfs\")\n    install_dir = cloud_config.get(\"install_dir\", \"C:\\\\WSL\")\n\n    if not rootfs:\n        print(f\"Error: Rootfs image path not configured in YAML for wsl2. Cannot import {name}.\")\n        return \"error\"\n\n    try:\n        # wsl --import &lt;Distro&gt; &lt;InstallLocation&gt; &lt;FileName&gt;\n        self._run_command([\"wsl\", \"--import\", name, install_dir, rootfs])\n        self._run_command([\"wsl\", \"-d\", name])\n        return name\n    except subprocess.CalledProcessError:\n        return \"error\"\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.Wsl2Provider.stop","title":"stop","text":"<pre><code>stop(name=None)\n</code></pre> <p>Stops (terminates) a WSL2 distribution.</p> Source code in <code>src/cloudmesh/ai/vm/local/Wsl2Manager.py</code> <pre><code>def stop(self, name: Optional[str] = None) -&gt; bool:\n    \"\"\"\n    Stops (terminates) a WSL2 distribution.\n    \"\"\"\n    if not name:\n        print(\"Error: Distro name is required to stop.\")\n        return False\n\n    try:\n        self._run_command([\"wsl\", \"--terminate\", name])\n        return True\n    except subprocess.CalledProcessError:\n        return False\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.Wsl2Provider.suspend","title":"suspend","text":"<pre><code>suspend(name=None)\n</code></pre> <p>WSL2 doesn't have a 'suspend' mode; we use terminate.</p> Source code in <code>src/cloudmesh/ai/vm/local/Wsl2Manager.py</code> <pre><code>def suspend(self, name: Optional[str] = None) -&gt; bool:\n    \"\"\"\n    WSL2 doesn't have a 'suspend' mode; we use terminate.\n    \"\"\"\n    return self.stop(name)\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm-functions","title":"Functions:","text":""},{"location":"api.html#cloudmesh.ai.command.vm.config","title":"config","text":"<pre><code>config()\n</code></pre> <p>Display the current cloud configuration file.</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>@vm_group.command(name=\"config\")\ndef config():\n    \"\"\"Display the current cloud configuration file.\"\"\"\n    click.echo(f\"Configuration file location: {CONFIG_PATH}\\n\")\n    try:\n        with open(CONFIG_PATH, 'r') as f:\n            click.echo(f.read())\n    except FileNotFoundError:\n        click.echo(f\"Configuration file not found at {CONFIG_PATH}\", err=True)\n    except Exception as e:\n        click.echo(f\"Error reading configuration file: {e}\", err=True)\n\n    if os.path.exists(CONFIG_PATH):\n        click.echo(f\"Configuration file already exists at {CONFIG_PATH}\")\n        return\n\n    click.echo(\"Sample configuration file:\")\n    click.echo(\"-\" * 20)\n    click.echo(yaml.dump(sample_config, default_flow_style=False))\n    click.echo(\"-\" * 20)\n\n    if click.confirm(\"Do you want to save this as your default configuration?\"):\n        try:\n            os.makedirs(os.path.dirname(CONFIG_PATH), exist_ok=True)\n            with open(CONFIG_PATH, \"w\") as f:\n                yaml.dump(sample_config, f, default_flow_style=False)\n            click.echo(f\"Configuration saved to {CONFIG_PATH}\")\n        except Exception as e:\n            click.echo(f\"Error saving configuration: {e}\", err=True)\n    else:\n        click.echo(\"Setup cancelled.\")\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.delete","title":"delete","text":"<pre><code>delete(ctx, name)\n</code></pre> <p>Deletes a VM</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>@vm_group.command()\n@click.option(\"--name\", help=\"Name of the VM\")\n@click.pass_context\ndef delete(ctx, name):\n    \"\"\"Deletes a VM\"\"\"\n    provider = get_active_provider(ctx)\n\n    vm_name = name or state.get_last_vm()\n    if not vm_name:\n        raise click.ClickException(\"No VM name provided and no last-used VM found.\")\n\n    if provider.delete(name=vm_name):\n        click.echo(f\"VM {vm_name} deleted.\")\n    else:\n        click.echo(f\"Failed to delete VM {vm_name}.\", err=True)\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.flavors","title":"flavors","text":"<pre><code>flavors(ctx)\n</code></pre> <p>Lists available hardware profiles</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>@vm_group.command()\n@click.pass_context\ndef flavors(ctx):\n    \"\"\"Lists available hardware profiles\"\"\"\n    provider = get_active_provider(ctx)\n    flavors = provider.get_flavors()\n    if not flavors:\n        click.echo(f\"No flavors found or not supported by provider '{provider.cloud_name}'.\")\n        return\n\n    headers = flavors[0].keys()\n    header_line = \"  \".join(f\"{h:&lt;15}\" for h in headers)\n    click.echo(header_line)\n    click.echo(\"-\" * len(header_line))\n    for f in flavors:\n        click.echo(\"  \".join(f\"{str(v):&lt;15}\" for v in f.values()))\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.get_active_provider","title":"get_active_provider","text":"<pre><code>get_active_provider(ctx)\n</code></pre> <p>Helper to resolve the provider based on override or default.</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>def get_active_provider(ctx):\n    \"\"\"Helper to resolve the provider based on override or default.\"\"\"\n    cloud_name = ctx.obj.cloud_override or state.config.default_cloud\n    if not cloud_name:\n        raise click.ClickException(\"No default cloud set. Use 'cmc vm set &lt;cloud&gt;' or --cloud &lt;cloud&gt;.\")\n\n    try:\n        provider = factory.create(cloud_name, state.config)\n        provider.cloud_name = cloud_name\n        return provider\n    except Exception as e:\n        raise click.ClickException(str(e))\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.hello","title":"hello","text":"<pre><code>hello()\n</code></pre> <p>Hello command</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>@vm_group.command()\ndef hello():\n    \"\"\"Hello command\"\"\"\n    click.echo(\"Hello from vm!\")\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.images","title":"images","text":"<pre><code>images(ctx)\n</code></pre> <p>Lists available images</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>@vm_group.command()\n@click.pass_context\ndef images(ctx):\n    \"\"\"Lists available images\"\"\"\n    provider = get_active_provider(ctx)\n    images = provider.get_images()\n    if not images:\n        click.echo(f\"No images found or not supported by provider '{provider.cloud_name}'.\")\n        return\n\n    headers = images[0].keys()\n    header_line = \"  \".join(f\"{h:&lt;15}\" for h in headers)\n    click.echo(header_line)\n    click.echo(\"-\" * len(header_line))\n    for i in images:\n        click.echo(\"  \".join(f\"{str(v):&lt;15}\" for v in i.values()))\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.info","title":"info","text":"<pre><code>info(ctx, name)\n</code></pre> <p>Gets detailed information about a VM</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>@vm_group.command()\n@click.argument(\"name\")\n@click.pass_context\ndef info(ctx, name):\n    \"\"\"Gets detailed information about a VM\"\"\"\n    provider = get_active_provider(ctx)\n    vm_info = provider.info(name)\n\n    if \"error\" in vm_info:\n        click.echo(f\"Error: {vm_info['error']}\", err=True)\n        return\n\n    click.echo(f\"\\nInformation for VM: {name}\")\n    click.echo(\"-\" * 30)\n    for key, value in vm_info.items():\n        click.echo(f\"{key:&lt;15}: {value}\")\n    click.echo(\"-\" * 30)\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.keys","title":"keys","text":"<pre><code>keys(ctx)\n</code></pre> <p>Lists available SSH keys</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>@vm_group.command()\n@click.pass_context\ndef keys(ctx):\n    \"\"\"Lists available SSH keys\"\"\"\n    provider = get_active_provider(ctx)\n    keys = provider.get_keys()\n    if not keys:\n        click.echo(f\"No keys found or not supported by provider '{provider.cloud_name}'.\")\n        return\n\n    headers = keys[0].keys()\n    header_line = \"  \".join(f\"{h:&lt;15}\" for h in headers)\n    click.echo(header_line)\n    click.echo(\"-\" * len(header_line))\n    for k in keys:\n        click.echo(\"  \".join(f\"{str(v):&lt;15}\" for v in k.values()))\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.list_vms","title":"list_vms","text":"<pre><code>list_vms(ctx, format_json, format_yaml, format_csv, format_table)\n</code></pre> <p>Lists VMs</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>@vm_group.command(name=\"list\")\n@click.option(\"--json\", \"format_json\", is_flag=True, help=\"Output in JSON\")\n@click.option(\"--yaml\", \"format_yaml\", is_flag=True, help=\"Output in YAML\")\n@click.option(\"--csv\", \"format_csv\", is_flag=True, help=\"Output in CSV\")\n@click.option(\"--table\", \"format_table\", is_flag=True, default=True, help=\"Output in Table\")\n@click.pass_context\ndef list_vms(ctx, format_json, format_yaml, format_csv, format_table):\n    \"\"\"Lists VMs\"\"\"\n    provider = get_active_provider(ctx)\n    vms = provider.list()\n\n    if not vms:\n        click.echo(\"No VMs found.\")\n        return\n\n    if format_json:\n        import json\n        click.echo(json.dumps(vms, indent=2))\n    elif format_yaml:\n        click.echo(yaml.dump(vms))\n    elif format_csv:\n        headers = vms[0].keys()\n        click.echo(\",\".join(headers))\n        for vm in vms:\n            click.echo(\",\".join(str(v) for v in vm.values()))\n    else:\n        headers = vms[0].keys()\n        header_line = \"  \".join(f\"{h:&lt;15}\" for h in headers)\n        click.echo(header_line)\n        click.echo(\"-\" * len(header_line))\n        for vm in vms:\n            click.echo(\"  \".join(f\"{str(v):&lt;15}\" for v in vm.values()))\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.login","title":"login","text":"<pre><code>login(ctx, name)\n</code></pre> <p>Logs into a VM</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>@vm_group.command()\n@click.option(\"--name\", help=\"Name of the VM\")\n@click.pass_context\ndef login(ctx, name):\n    \"\"\"Logs into a VM\"\"\"\n    provider = get_active_provider(ctx)\n\n    vm_name = name or state.get_last_vm()\n    if not vm_name:\n        raise click.ClickException(\"No VM name provided and no last-used VM found.\")\n\n    if provider.login(name=vm_name):\n        click.echo(f\"Logged into {vm_name}.\")\n    else:\n        click.echo(f\"Failed to login to {vm_name}.\", err=True)\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.providers","title":"providers","text":"<pre><code>providers()\n</code></pre> <p>Lists all supported VM providers and their status on this system</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>@vm_group.command()\ndef providers():\n    \"\"\"Lists all supported VM providers and their status on this system\"\"\"\n    providers_list = sorted(factory._registry.keys())\n    if not providers_list:\n        click.echo(\"No providers registered.\")\n        return\n\n    # Table header\n    header = f\"{'Provider':&lt;15} {'Supported':&lt;12} {'Enabled':&lt;12} {'OS-YAML':&lt;12} {'CM-YAML':&lt;12} {'Reason'}\"\n    click.echo(header)\n    click.echo(\"-\" * 90)\n\n    import re\n    import os\n    import yaml\n\n    os_yaml_path = os.path.expanduser(\"~/.config/openstack/clouds.yaml\")\n    os_clouds = {}\n    if os.path.exists(os_yaml_path):\n        try:\n            with open(os_yaml_path, 'r') as f:\n                data = yaml.safe_load(f) or {}\n                os_clouds = data.get(\"clouds\", data)\n        except Exception:\n            pass\n\n    for p_name in providers_list:\n        status = \"\u274c\"\n        reason = \"\"\n\n        # Check config presence in CloudMesh\n        cloud_cfg_obj = state.config.clouds.get(p_name)\n        cm_has = \"\u2705\" if cloud_cfg_obj is not None else \"\u274c\"\n\n        # Check presence in OpenStack clouds.yaml\n        os_has = \"\u2705\" if p_name in os_clouds else \"\u274c\"\n\n        # Determine Enabled value\n        if cloud_cfg_obj is None:\n            enabled_val = \"\u26aa\"\n        else:\n            val = getattr(cloud_cfg_obj, \"enabled\", None)\n\n            if isinstance(val, str):\n                is_enabled = val.lower() == \"true\"\n            elif val is None:\n                is_enabled = True\n            else:\n                is_enabled = bool(val)\n\n            enabled_val = \"\u2705\" if is_enabled else \"\u274c\"\n\n        # OpenStack providers MUST have config in either YAML\n        if p_name in [\"jetstream\", \"chameleon\", \"openstack\"]:\n            if cm_has == \"\u274c\" and os_has == \"\u274c\":\n                click.echo(f\"{p_name:&lt;15} {status:&lt;12} {enabled_val:&lt;12} {os_has:&lt;12} {cm_has:&lt;12} Missing config in all YAMLs\")\n                continue\n\n        try:\n            # Try to create the provider to check its requirements\n            provider = factory.create(p_name, state.config)\n            if provider.check_requirements():\n                status = \"\u2705\"\n                reason = \"Supported\"\n                if cm_has == \"\u274c\":\n                    reason += \" (using defaults)\"\n            else:\n                reason = \"Requirements not met\"\n        except TypeError as e:\n            err_msg = str(e)\n            if \"Can't instantiate abstract class\" in err_msg:\n                methods_match = re.search(r\"abstract methods? '([^']+)'\", err_msg)\n                if not methods_match:\n                    methods_match = re.search(r\"implementing new methods: ([^.\\n]+)\", err_msg)\n\n                methods = methods_match.group(1) if methods_match else \"unknown\"\n                reason = f\"Not implemented: {methods}\"\n            else:\n                reason = err_msg\n        except Exception as e:\n            reason = str(e)\n\n        click.echo(f\"{p_name:&lt;15} {status:&lt;12} {enabled_val:&lt;12} {os_has:&lt;12} {cm_has:&lt;12} {reason}\")\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.register","title":"register","text":"<pre><code>register(cli)\n</code></pre> <p>Registers the vm command group to the main CLI.</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>def register(cli):\n    \"\"\"Registers the vm command group to the main CLI.\"\"\"\n    cli.add_command(vm_group, name=\"vm\")\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.reservation","title":"reservation","text":"<pre><code>reservation(ctx, name, node_type, count, start, end, duration)\n</code></pre> <p>Creates a reservation in Chameleon Cloud</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>@vm_group.command()\n@click.option(\"--name\", required=True, help=\"Name of the reservation\")\n@click.option(\"--node-type\", required=True, help=\"Type of node (e.g. compute_skylake)\")\n@click.option(\"--count\", type=int, required=True, help=\"Number of nodes\")\n@click.option(\"--start\", help=\"Start date (YYYY-MM-DD HH:MM)\")\n@click.option(\"--end\", help=\"End date (YYYY-MM-DD HH:MM)\")\n@click.option(\"--duration\", type=int, help=\"Duration of the lease in days\")\n@click.pass_context\ndef reservation(ctx, name, node_type, count, start, end, duration):\n    \"\"\"Creates a reservation in Chameleon Cloud\"\"\"\n    provider = get_active_provider(ctx)\n\n    if not hasattr(provider, \"create_reservation\"):\n        click.echo(\"Error: Current provider does not support reservations.\", err=True)\n        return\n\n    if provider.create_reservation(name=name, node_type=node_type, count=count, start_date=start, end_date=end, duration=duration):\n        click.echo(f\"Reservation {name} created successfully.\")\n    else:\n        click.echo(f\"Failed to create reservation {name}.\", err=True)\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.restart","title":"restart","text":"<pre><code>restart(ctx, name)\n</code></pre> <p>Restarts a VM</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>@vm_group.command()\n@click.option(\"--name\", help=\"Name of the VM\")\n@click.pass_context\ndef restart(ctx, name):\n    \"\"\"Restarts a VM\"\"\"\n    provider = get_active_provider(ctx)\n\n    vm_name = name or state.get_last_vm()\n    if not vm_name:\n        raise click.ClickException(\"No VM name provided and no last-used VM found.\")\n\n    if provider.restart(name=vm_name):\n        click.echo(f\"VM {vm_name} restarted.\")\n    else:\n        click.echo(f\"Failed to restart VM {vm_name}.\", err=True)\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.run","title":"run","text":"<pre><code>run(ctx, name, command)\n</code></pre> <p>Executes a command on a VM</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>@vm_group.command(name=\"run\")\n@click.option(\"--name\", help=\"Name of the VM\")\n@click.argument(\"command\")\n@click.pass_context\ndef run(ctx, name, command):\n    \"\"\"Executes a command on a VM\"\"\"\n    provider = get_active_provider(ctx)\n\n    vm_name = name or state.get_last_vm()\n    if not vm_name:\n        raise click.ClickException(\"No VM name provided and no last-used VM found.\")\n\n    output = provider.run_command(vm_name, command)\n    click.echo(output)\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.security_groups","title":"security_groups","text":"<pre><code>security_groups(ctx)\n</code></pre> <p>Lists available security groups</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>@vm_group.command()\n@click.pass_context\ndef security_groups(ctx):\n    \"\"\"Lists available security groups\"\"\"\n    provider = get_active_provider(ctx)\n    groups = provider.get_security_groups()\n    if not groups:\n        click.echo(f\"No security groups found or not supported by provider '{provider.cloud_name}'.\")\n        return\n\n    headers = groups[0].keys()\n    header_line = \"  \".join(f\"{h:&lt;15}\" for h in headers)\n    click.echo(header_line)\n    click.echo(\"-\" * len(header_line))\n    for g in groups:\n        click.echo(\"  \".join(f\"{str(v):&lt;15}\" for v in g.values()))\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.set_cloud","title":"set_cloud","text":"<pre><code>set_cloud(cloud)\n</code></pre> <p>Set the default cloud provider</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>@vm_group.command(name=\"set\")\n@click.argument(\"cloud\")\ndef set_cloud(cloud):\n    \"\"\"Set the default cloud provider\"\"\"\n    state.config.default_cloud = cloud\n    state.save()\n    click.echo(f\"Default cloud set to: {cloud}\")\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.setup","title":"setup","text":"<pre><code>setup()\n</code></pre> <p>Initialize a sample configuration file</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>@vm_group.command(name=\"setup\")\ndef setup():\n    \"\"\"Initialize a sample configuration file\"\"\"\n    sample_config = {\n        \"username\": \"cloudmesh_user\",\n        \"counter\": 0,\n        \"default_cloud\": \"multipass\",\n        \"clouds\": {\n            \"multipass\": {\n                \"image\": \"ubuntu-22.04\"\n            },\n            \"aws\": {\n                \"region\": \"us-east-1\",\n                \"image\": \"ami-xxxxxxxxxxxxxxxxx\"\n            },\n            \"jetstream\": {\n                \"image\": \"Featured-Minimal-Ubuntu24\",\n                \"flavor\": \"m3.tiny\"\n            },\n            \"chameleon\": {\n                \"image\": \"CC-Ubuntu24.04\",\n                \"flavor\": \"m1.small\",\n                \"region\": \"CHI@TACC\"\n            }\n        }\n    }\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.ssh_config","title":"ssh_config","text":"<pre><code>ssh_config(ctx)\n</code></pre> <p>Suggests SSH config entries for existing VMs</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>@vm_group.command()\n@click.pass_context\ndef ssh_config(ctx):\n    \"\"\"Suggests SSH config entries for existing VMs\"\"\"\n    provider = get_active_provider(ctx)\n    vms = provider.list()\n\n    if not vms:\n        click.echo(\"No existing VMs found to generate config for.\")\n        return\n\n    click.echo(\"\\nAdd the following to your ~/.ssh/config:\\n\")\n    for vm in vms:\n        name = vm.get(\"Name\")\n        ip = vm.get(\"IP\", \" &lt;VM_IP&gt;\")\n\n        click.echo(f\"Host {name}\")\n        click.echo(f\"    HostName {ip}\")\n        click.echo(f\"    User {state.config.username}\")\n        click.echo(f\"    IdentityFile ~/.ssh/id_rsa\")\n        click.echo(\"\")\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.start","title":"start","text":"<pre><code>start(ctx, name)\n</code></pre> <p>Starts a VM</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>@vm_group.command()\n@click.option(\"--name\", help=\"Name of the VM\")\n@click.pass_context\ndef start(ctx, name):\n    \"\"\"Starts a VM\"\"\"\n    provider = get_active_provider(ctx)\n\n    vm_name = name\n    if not vm_name:\n        username = state.config.username\n        counter = state.increment_counter()\n        # Ensure a hyphen between username and counter, and replace underscores in username\n        vm_name = f\"{username}-{counter}\".replace(\"_\", \"-\")\n\n    try:\n        result_name = provider.start(name=vm_name)\n        state.set_last_vm(result_name)\n        click.echo(f\"VM {result_name} started successfully.\")\n    except CloudMeshError as e:\n        click.echo(f\"Error: {e}\", err=True)\n    except Exception as e:\n        logger.error(f\"Unexpected error: {e}\")\n        click.echo(f\"An unexpected error occurred: {e}\", err=True)\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.stop","title":"stop","text":"<pre><code>stop(ctx, name)\n</code></pre> <p>Stops a VM</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>@vm_group.command()\n@click.option(\"--name\", help=\"Name of the VM\")\n@click.pass_context\ndef stop(ctx, name):\n    \"\"\"Stops a VM\"\"\"\n    provider = get_active_provider(ctx)\n\n    vm_name = name or state.get_last_vm()\n    if not vm_name:\n        raise click.ClickException(\"No VM name provided and no last-used VM found.\")\n\n    if provider.stop(name=vm_name):\n        click.echo(f\"VM {vm_name} stopped.\")\n    else:\n        click.echo(f\"Failed to stop VM {vm_name}.\", err=True)\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.suspend","title":"suspend","text":"<pre><code>suspend(ctx, name)\n</code></pre> <p>Suspends a VM</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>@vm_group.command()\n@click.option(\"--name\", help=\"Name of the VM\")\n@click.pass_context\ndef suspend(ctx, name):\n    \"\"\"Suspends a VM\"\"\"\n    provider = get_active_provider(ctx)\n\n    vm_name = name or state.get_last_vm()\n    if not vm_name:\n        raise click.ClickException(\"No VM name provided and no last-used VM found.\")\n\n    if provider.suspend(name=vm_name):\n        click.echo(f\"VM {vm_name} suspended.\")\n    else:\n        click.echo(f\"Failed to suspend VM {vm_name}.\", err=True)\n</code></pre>"},{"location":"api.html#cloudmesh.ai.command.vm.vm_group","title":"vm_group","text":"<pre><code>vm_group(ctx, cloud, debug)\n</code></pre> <p>VM management commands</p> Source code in <code>src/cloudmesh/ai/command/vm.py</code> <pre><code>@click.group(name=\"vm\")\n@click.option(\"--cloud\", help=\"Override the default cloud provider\")\n@click.option(\"--debug\", is_flag=True, help=\"Enable debug logging\")\n@click.pass_context\ndef vm_group(ctx, cloud, debug):\n    \"\"\"VM management commands\"\"\"\n    ctx.obj = VMContext()\n    ctx.obj.cloud_override = cloud\n    if debug:\n        logger.setLevel(logging.DEBUG)\n        logger.debug(f\"Debug mode enabled. Using cloud: {cloud or state.config.default_cloud}\")\n</code></pre>"},{"location":"architecture.html","title":"Architecture","text":""},{"location":"architecture.html#architecture","title":"Architecture","text":"<p>`cloudmesh-ai-vm` is built on the Provider Pattern, ensuring that the core CLI logic is completely decoupled from the specific implementation details of any cloud provider.</p>"},{"location":"architecture.html#the-provider-hierarchy","title":"The Provider Hierarchy","text":"<p>The system uses a multi-layered inheritance structure to maximize code reuse.</p>"},{"location":"architecture.html#1-cloudbasemanager-abc","title":"1. CloudBaseManager (ABC)","text":"<p>The `CloudBaseManager` is an Abstract Base Class (ABC) that defines the \"contract\" for all providers. It ensures that every manager implements a consistent set of methods:</p> <ul> <li>`start()<code>, \\</code>stop()<code>, \\</code>delete()<code>, \\</code>list()<code>, \\</code>login()<code>, \\</code>suspend()<code>, \\</code>restart()`</li> <li>`get_flavors()<code>, \\</code>get_keys()<code>, \\</code>get_security_groups()`</li> </ul>"},{"location":"architecture.html#2-libcloudmanager-intermediate-base","title":"2. LibcloudManager (Intermediate Base)","text":"<p>Because many clouds (AWS, Azure, Google, and some OpenStack) are supported by the `libcloud` library, we use `LibcloudManager` to implement the generic logic for these providers. This prevents us from writing the same `stop` or `list` logic multiple times.</p>"},{"location":"architecture.html#3-concrete-providers","title":"3. Concrete Providers","text":"<p>Concrete classes (e.g., `AwsManager`, `MultipassManager`) inherit from either `LibcloudManager` or `CloudBaseManager`. Their primary responsibility is:</p> <ul> <li>Authentication: Implementing `_get_driver()` to handle API keys and credentials.</li> <li>Specialization: Implementing provider-specific features (like Chameleon's reservations).</li> </ul>"},{"location":"architecture.html#data-flow","title":"Data Flow","text":"<ol> <li>CLI Layer (`main.py`): Receives a command (e.g., `vm start`).</li> <li>Factory: Looks up the active cloud in `clouds.yaml` and instantiates the corresponding Provider class.</li> <li>Provider: Executes the requested operation using the cloud's API (via `libcloud` or native tools).</li> <li>CLI Layer: Formats the result and displays it to the user.</li> </ol>"},{"location":"architecture.html#extensibility","title":"Extensibility","text":"<p>To add a new provider:</p> <ol> <li>Create a new class inheriting from `CloudBaseManager` (or `LibcloudManager`).</li> <li>Implement the required abstract methods.</li> <li>Add the provider mapping to the `PROVIDERS` registry in `main.py`.</li> </ol>"},{"location":"cli.html","title":"CLI Reference","text":""},{"location":"cli.html#cli-reference","title":"CLI Reference","text":"<p>The `cmc` tool provides a consistent interface for managing VMs across all supported providers.</p>"},{"location":"cli.html#global-command-structure","title":"Global Command Structure","text":"<p>```bash cmc vm  [options] ```"},{"location":"cli.html#global-options","title":"Global Options","text":"<p>The `vm` group supports the following global option:</p> <ul> <li>`--cloud `: Overrides the default cloud provider for the duration of the command. For example, `cmc vm start --cloud aws` will start a VM on AWS even if Multipass is your default."},{"location":"cli.html#commands","title":"Commands","text":""},{"location":"cli.html#1-cloud-management","title":"1. Cloud Management","text":""},{"location":"cli.html#set","title":"`set`","text":"<p>Sets the default cloud provider used for all subsequent commands.</p> <ul> <li>Usage: `cmc vm set ` <li>Example: `cmc vm set aws`</li>"},{"location":"cli.html#2-vm-lifecycle","title":"2. VM Lifecycle","text":"<p>Many lifecycle commands now support Contextual Memory. If the `--name` option is omitted, the tool will target the last VM that was successfully started.</p>"},{"location":"cli.html#start","title":"`start`","text":"<p>Launches a new VM.</p> <ul> <li>Options:</li> <li>`--name `: Specify a custom name. If omitted, the tool uses ``. <li>Examples:</li> <li>Default: `cmc vm start`</li> <li>Custom Name: `cmc vm start --name my-web-server`</li> <li>Cloud Override: `cmc vm start --cloud aws`</li>"},{"location":"cli.html#stop","title":"`stop`","text":"<p>Stops a running VM.</p> <ul> <li>Options:</li> <li>`--name `: Name of the VM to stop. If omitted, the last started VM is used. <li>Example: `cmc vm stop` (Stops last VM)</li>"},{"location":"cli.html#delete","title":"`delete`","text":"<p>Permanently removes a VM.</p> <ul> <li>Options:</li> <li>`--name `: Name of the VM to delete. If omitted, the last started VM is used. <li>Example: `cmc vm delete --name my-web-server`</li>"},{"location":"cli.html#suspend","title":"`suspend`","text":"<p>Suspends a VM to disk (if supported by the provider).</p> <ul> <li>Options:</li> <li>`--name `: Name of the VM to suspend. If omitted, the last started VM is used. <li>Example: `cmc vm suspend`</li>"},{"location":"cli.html#restart","title":"`restart`","text":"<p>Reboots a VM.</p> <ul> <li>Options:</li> <li>`--name `: Name of the VM to restart. If omitted, the last started VM is used. <li>Example: `cmc vm restart`</li>"},{"location":"cli.html#login","title":"`login`","text":"<p>Provides connection details or logs into the VM.</p> <ul> <li>Options:</li> <li>`--name `: Name of the VM. If omitted, the last started VM is used. <li>Example: `cmc vm login`</li>"},{"location":"cli.html#3-inspection-discovery","title":"3. Inspection &amp; Discovery","text":""},{"location":"cli.html#list","title":"`list`","text":"<p>Lists all VMs managed by the current provider.</p> <ul> <li>Options:</li> <li>`--table`: (Default) Prints a formatted table.</li> <li>`--json`: Prints output in JSON format.</li> <li>`--yaml`: Prints output in YAML format.</li> <li>`--csv`: Prints output in CSV format.</li> <li>Example: `cmc vm list --json`</li> </ul>"},{"location":"cli.html#flavors","title":"`flavors`","text":"<p>Lists available hardware profiles for the current cloud.</p> <ul> <li>Example: `cmc vm flavors`</li> </ul>"},{"location":"cli.html#keys","title":"`keys`","text":"<p>Lists available SSH keys in the current cloud.</p> <ul> <li>Example: `cmc vm keys`</li> </ul>"},{"location":"cli.html#security-groups","title":"`security-groups`","text":"<p>Lists available security groups for the current cloud.</p> <ul> <li>Example: `cmc vm security-groups`</li> </ul>"},{"location":"cli.html#4-networking-ssh","title":"4. Networking &amp; SSH","text":""},{"location":"cli.html#ssh-config","title":"`ssh-config`","text":"<p>Generates suggested SSH configuration entries for all existing VMs in the current cloud. This allows you to connect using `ssh ` without modifying your config file automatically. <ul> <li>Example: `cmc vm ssh-config`</li> </ul>"},{"location":"cli.html#5-specialized-commands","title":"5. Specialized Commands","text":""},{"location":"cli.html#reservation-chameleon-only","title":"`reservation` (Chameleon Only)","text":"<p>Creates a hardware reservation (lease) in Chameleon Cloud.</p> <ul> <li>Options:</li> <li>`--name `: (Required) Name of the reservation. <li>`--node-type `: (Required) e.g., `compute_skylake`. <li>`--count `: (Required) Number of nodes. <li>`--start `: Start date (YYYY-MM-DD HH:MM). <li>`--end `: End date (YYYY-MM-DD HH:MM). <li>`--duration `: Duration of the lease in days. <li>Examples:</li> <li>Duration based: `cmc vm reservation --name my-lease --node-type compute_skylake --count 1 --duration 2`</li> <li>Date based: `cmc vm reservation --name my-lease --node-type compute_skylake --count 1 --start \"2026-10-01 08:00\" --end \"2026-10-02 08:00\"`</li>"},{"location":"configuration.html","title":"Configuration","text":""},{"location":"configuration.html#configuration","title":"Configuration","text":"<p>`cloudmesh-ai-vm` uses a centralized YAML configuration file located at `~/.config/cloudmesh/clouds.yaml`. This file stores your global settings, provider credentials, and the state of your VM counters.</p>"},{"location":"configuration.html#file-structure","title":"File Structure","text":"<p>Below is a comprehensive example of a `clouds.yaml` file covering all supported providers.</p> <p>```yaml</p>"},{"location":"configuration.html#global-settings","title":"Global Settings","text":"<p>username: gregor counter: 0 default_cloud: multipass last_vm: gregor0</p> <p>clouds:   # --- Local Providers ---   multipass:     image: 22.04     cpus: 2     memory: 4GiB     disk: 20GiB</p> <p>wsl2:     distro: Ubuntu-22.04     ssh_link: true  # Symbolically link host .ssh directory</p> <p>vbox:     image: ubuntu-server-22.04.iso     memory: 2048     cpus: 1</p> <p># --- OpenStack Providers ---   jetstream:     flavour: m1.small     image: ubuntu-22.04     auth: /home/user/.config/jetstream/auth.yaml     security_group: default</p> <p>chameleon:     flavour: c1.small     image: rocky-linux-8     auth: /home/user/.config/chameleon/auth.yaml     project_name: CH-XXXXXX     site: CHI@TACC</p> <p># --- Hyperscalers (libcloud) ---   aws:     access_key: AKIA...     secret_key: wJal...     region: us-east-1     image: ami-0c55b159cbfafe1f0     size: t2.micro</p> <p>azure:     tenant_id: xxxx-xxxx-xxxx     subscription_id: xxxx-xxxx-xxxx     client_id: xxxx-xxxx-xxxx     client_secret: xxxx-xxxx-xxxx     image: ubuntu-22.04     size: Standard_DS1_v2</p> <p>google:     project_id: my-gcp-project     private_key: /home/user/.config/google/service-account.json     image: ubuntu-2204-lts     size: n1-standard-1 ```</p>"},{"location":"configuration.html#field-descriptions","title":"Field Descriptions","text":""},{"location":"configuration.html#global-fields","title":"Global Fields","text":"<ul> <li>`username`: Used as a prefix for automatically generated VM names (e.g., `gregor0`).</li> <li>`counter`: Tracks the number of VMs started. Incremented automatically on every `cmc vm start` without a name.</li> <li>`default_cloud`: The provider used when no specific cloud is mentioned.</li> <li>`last_vm`: Tracks the name of the last VM successfully started. This allows lifecycle commands (stop, delete, etc.) to target the VM without specifying `--name`.</li> </ul>"},{"location":"configuration.html#provider-specific-fields","title":"Provider-Specific Fields","text":"<ul> <li>`auth`: Path to a YAML file containing credentials (used by OpenStack).</li> <li>`image`: The OS image identifier for the specific provider.</li> <li>`size` / `flavour`: The hardware profile (CPU/RAM) for the VM.</li> </ul>"},{"location":"contributing.html","title":"Contributing","text":""},{"location":"contributing.html#contributing","title":"Contributing","text":"<p>We welcome contributions to make `cloudmesh-ai-vm` more powerful and supportive of more clouds!</p>"},{"location":"contributing.html#how-to-contribute","title":"How to Contribute","text":""},{"location":"contributing.html#adding-a-new-provider","title":"Adding a New Provider","text":"<ol> <li>Check libcloud: See if the provider is already supported by `libcloud`. If so, inherit from `LibcloudManager`.</li> <li>Implement Interface: If it's a custom provider, inherit from `CloudBaseManager` and implement all abstract methods.</li> <li>Add Config: Update the `clouds.yaml` example in the documentation to show required credentials.</li> <li>Register: Add your provider to the `PROVIDERS` map in `src/main.py`.</li> <li>Test: Create a new test file in `tests/` using `unittest.mock`.</li> </ol>"},{"location":"contributing.html#development-workflow","title":"Development Workflow","text":"<ul> <li>Branching: Create a feature branch from `main`.</li> <li>Commits: Use descriptive commit messages.</li> <li>PRs: Submit a Pull Request with a clear description of the changes and examples of how to use the new feature.</li> </ul>"},{"location":"contributing.html#coding-standards","title":"Coding Standards","text":"<ul> <li>Follow PEP 8 guidelines.</li> <li>Use type hinting for all method signatures.</li> <li>Ensure all new functionality is covered by unit tests.</li> </ul>"},{"location":"installation.html","title":"Installation","text":""},{"location":"installation.html#installation-guide","title":"Installation Guide","text":"<p>This guide will walk you through the process of installing `cloudmesh-ai-vm` on your local machine.</p>"},{"location":"installation.html#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.x: Ensure you have Python 3.8 or higher installed.</li> <li>Pip: The Python package installer.</li> </ul>"},{"location":"installation.html#setup-steps","title":"Setup Steps","text":""},{"location":"installation.html#1-clone-the-repository","title":"1. Clone the Repository","text":"<p>```bash git clone https://github.com/your-repo/cloudmesh-ai-vm.git cd cloudmesh-ai-vm ```</p>"},{"location":"installation.html#2-install-dependencies","title":"2. Install Dependencies","text":"<p>We recommend using a virtual environment to avoid dependency conflicts.</p> <p>```bash python -m venv venv source venv/bin/activate  # On Windows: venv\\Scripts\\activate pip install -r requirements.txt ```</p>"},{"location":"installation.html#3-install-the-cli-tool","title":"3. Install the CLI Tool","text":"<p>Install the package in editable mode so you can make changes to the source code:</p> <p>```bash pip install -e . ```</p>"},{"location":"installation.html#verifying-installation","title":"Verifying Installation","text":"<p>Run the following command to ensure the CLI is installed and working:</p> <p>```bash cmc --help ```</p> <p>If you see the help menu, you are ready to go!</p>"},{"location":"readme.html","title":"cloudmesh-ai-vm","text":""},{"location":"readme.html#cloudmesh-ai-vm","title":"cloudmesh-ai-vm","text":"<p>Quick Links: - API Reference - Full technical documentation of all modules.</p> <p>This is a CMC extension for Cloudmesh AI.</p>"},{"location":"readme.html#installation","title":"Installation","text":"<pre><code>pip install .\n</code></pre>"},{"location":"readme.html#usage","title":"Usage","text":"<p>```bash cmc vm hello</p>"},{"location":"readme.html#core-dependencies","title":"Core Dependencies","text":"<p>This project depends on the following core components of the Cloudmesh AI ecosystem: - cloudmesh-ai-common - cloudmesh-ai-cmc</p>"},{"location":"providers/index.html","title":"Overview","text":""},{"location":"providers/index.html#providers-overview","title":"Providers Overview","text":"<p>`cloudmesh-ai-vm` supports a wide array of providers, categorized by their hosting model.</p>"},{"location":"providers/index.html#local-providers","title":"Local Providers","text":"<p>Ideal for rapid development, testing, and offline work.</p> <ul> <li>Multipass: Canonical's lightweight Ubuntu VM manager.</li> <li>WSL2: Windows Subsystem for Linux.</li> <li>VirtualBox: Full-featured headless VM management.</li> </ul>"},{"location":"providers/index.html#openstack-providers","title":"OpenStack Providers","text":"<p>Enterprise-grade cloud infrastructure often used in research and academic environments.</p> <ul> <li>Jetstream: High-performance computing (HPC) and cloud environments.</li> <li>Chameleon Cloud: Specialized in bare-metal and VM research with advanced reservation capabilities.</li> </ul>"},{"location":"providers/index.html#hyperscaler-providers","title":"Hyperscaler Providers","text":"<p>Global scale cloud services provided via the `libcloud` abstraction layer.</p> <ul> <li>AWS EC2: Amazon's Elastic Compute Cloud.</li> <li>Azure VMs: Microsoft Azure Virtual Machines.</li> <li>Google GCE: Google Compute Engine.</li> </ul>"},{"location":"providers/hyperscalers/aws.html","title":"AWS EC2","text":""},{"location":"providers/hyperscalers/aws.html#aws-ec2-provider","title":"AWS EC2 Provider","text":"<p>Integration for Amazon Elastic Compute Cloud (EC2) using the `libcloud` abstraction layer.</p>"},{"location":"providers/hyperscalers/aws.html#configuration","title":"Configuration","text":"<p>```yaml aws:   access_key: YOUR_ACCESS_KEY   secret_key: YOUR_SECRET_KEY   region: us-east-1   image: ami-0c55b159cbfafe1f0   size: t2.micro ```</p>"},{"location":"providers/hyperscalers/aws.html#usage-notes","title":"Usage Notes","text":"<ul> <li>Ensure your AWS IAM user has the necessary permissions to create and manage EC2 instances.</li> <li>Images (AMIs) are region-specific.</li> </ul>"},{"location":"providers/hyperscalers/aws.html#examples","title":"Examples","text":"<p>```bash cmc vm set aws cmc vm start --name aws-test-vm ```</p>"},{"location":"providers/hyperscalers/azure.html","title":"Azure VMs","text":""},{"location":"providers/hyperscalers/azure.html#azure-vms-provider","title":"Azure VMs Provider","text":"<p>Integration for Microsoft Azure Virtual Machines using the `libcloud` abstraction layer.</p>"},{"location":"providers/hyperscalers/azure.html#configuration","title":"Configuration","text":"<p>```yaml azure:   tenant_id: YOUR_TENANT_ID   subscription_id: YOUR_SUB_ID   client_id: YOUR_CLIENT_ID   client_secret: YOUR_CLIENT_SECRET   image: ubuntu-22.04   size: Standard_DS1_v2 ```</p>"},{"location":"providers/hyperscalers/azure.html#usage-notes","title":"Usage Notes","text":"<ul> <li>Authentication is handled via a Service Principal.</li> <li>Ensure you have created a Service Principal in your Azure AD.</li> </ul>"},{"location":"providers/hyperscalers/azure.html#examples","title":"Examples","text":"<p>```bash cmc vm set azure cmc vm start --name azure-node ```</p>"},{"location":"providers/hyperscalers/google.html","title":"Google GCE","text":""},{"location":"providers/hyperscalers/google.html#google-gce-provider","title":"Google GCE Provider","text":"<p>Integration for Google Compute Engine (GCE) using the `libcloud` abstraction layer.</p>"},{"location":"providers/hyperscalers/google.html#configuration","title":"Configuration","text":"<p>```yaml google:   project_id: YOUR_PROJECT_ID   private_key: /path/to/service-account.json   image: ubuntu-2204-lts   size: n1-standard-1 ```</p>"},{"location":"providers/hyperscalers/google.html#usage-notes","title":"Usage Notes","text":"<ul> <li>Requires a Google Cloud Service Account JSON key file.</li> <li>Ensure the Compute Engine API is enabled for your project.</li> </ul>"},{"location":"providers/hyperscalers/google.html#examples","title":"Examples","text":"<p>```bash cmc vm set google cmc vm start --name gcp-vm ```</p>"},{"location":"providers/local/multipass.html","title":"Multipass","text":""},{"location":"providers/local/multipass.html#multipass-provider","title":"Multipass Provider","text":"<p>Multipass is a lightweight VM manager for Ubuntu, developed by Canonical. It is the default provider for <code>cloudmesh-ai-vm</code> due to its speed and simplicity.</p>"},{"location":"providers/local/multipass.html#configuration","title":"Configuration","text":"<p>In your <code>clouds.yaml</code>, you can specify the resources for Multipass VMs:</p> <p>```yaml multipass:   image: 22.04   cpus: 2   memory: 4GiB   disk: 20GiB ```</p>"},{"location":"providers/local/multipass.html#key-features","title":"Key Features","text":"<ul> <li>Rapid Deployment: Start an Ubuntu VM in seconds.</li> <li>Resource Control: Fine-grained control over CPU, RAM, and Disk.</li> <li>Local Networking: VMs are accessible via local IP addresses.</li> </ul>"},{"location":"providers/local/multipass.html#examples","title":"Examples","text":"<p>```bash</p>"},{"location":"providers/local/multipass.html#set-multipass-as-default","title":"Set Multipass as default","text":"<p>cmc vm set multipass</p>"},{"location":"providers/local/multipass.html#start-a-vm-with-default-config","title":"Start a VM with default config","text":"<p>cmc vm start</p>"},{"location":"providers/local/multipass.html#list-local-multipass-vms","title":"List local Multipass VMs","text":"<p>cmc vm list ```</p>"},{"location":"providers/local/vbox.html","title":"VirtualBox","text":""},{"location":"providers/local/vbox.html#virtualbox-provider","title":"VirtualBox Provider","text":"<p>The VirtualBox provider manages headless VMs using the `VBoxManage` CLI tool.</p>"},{"location":"providers/local/vbox.html#configuration","title":"Configuration","text":"<p>```yaml vbox:   image: ubuntu-server-22.04.iso   memory: 2048   cpus: 1 ```</p>"},{"location":"providers/local/vbox.html#usage-notes","title":"Usage Notes","text":"<ul> <li>VMs are started in headless mode by default to maximize performance and reduce overhead.</li> <li>Ensure `VBoxManage` is in your system `PATH`.</li> </ul>"},{"location":"providers/local/vbox.html#examples","title":"Examples","text":"<p>```bash cmc vm set vbox cmc vm start ```</p>"},{"location":"providers/local/wsl2.html","title":"WSL2","text":""},{"location":"providers/local/wsl2.html#wsl2-provider","title":"WSL2 Provider","text":"<p>The WSL2 provider allows you to manage Windows Subsystem for Linux distributions as if they were VMs.</p>"},{"location":"providers/local/wsl2.html#configuration","title":"Configuration","text":"<p>```yaml wsl2:   distro: Ubuntu-22.04   ssh_link: true ```</p>"},{"location":"providers/local/wsl2.html#special-feature-ssh-linking","title":"Special Feature: SSH Linking","text":"<p>If `ssh_link: true` is set, `cloudmesh-ai-vm` will attempt to symbolically link your host's `.ssh` directory into the WSL2 distribution. This allows you to share your SSH keys and config between Windows and Linux seamlessly.</p>"},{"location":"providers/local/wsl2.html#examples","title":"Examples","text":"<p>```bash cmc vm set wsl2 cmc vm start ```</p>"},{"location":"providers/openstack/chameleon.html","title":"Chameleon","text":""},{"location":"providers/openstack/chameleon.html#chameleon-cloud-provider","title":"Chameleon Cloud Provider","text":"<p>Chameleon Cloud is a testbed for cloud research, offering both bare-metal and virtualized resources via OpenStack.</p>"},{"location":"providers/openstack/chameleon.html#configuration","title":"Configuration","text":"<p>```yaml chameleon:   flavour: c1.small   image: rocky-linux-8   auth: /path/to/chameleon/auth.yaml   project_name: CH-XXXXXX   site: CHI@TACC ```</p>"},{"location":"providers/openstack/chameleon.html#advanced-feature-hardware-reservations","title":"Advanced Feature: Hardware Reservations","text":"<p>Unlike standard clouds, Chameleon allows you to reserve specific hardware (e.g., specific CPU architectures) for a set period.</p>"},{"location":"providers/openstack/chameleon.html#creating-a-reservation","title":"Creating a Reservation","text":"<p>You can use the `reservation` command to create a lease.</p> <p>Example: 2-day lease for a Skylake node</p> <p>```bash cmc vm reservation --name my-research-lease --node-type compute_skylake --count 1 --duration 2 ```</p> <p>Example: Explicit dates</p> <p>```bash cmc vm reservation --name my-lease --node-type compute_skylake --count 1 --start \"2026-10-01 08:00\" --end \"2026-10-03 08:00\" ```</p>"},{"location":"providers/openstack/chameleon.html#examples","title":"Examples","text":"<p>```bash cmc vm set chameleon cmc vm start ```</p>"},{"location":"providers/openstack/jetstream.html","title":"Jetstream","text":""},{"location":"providers/openstack/jetstream.html#jetstream-provider","title":"Jetstream Provider","text":"<p>Jetstream provides high-performance computing (HPC) and cloud environments based on OpenStack.</p>"},{"location":"providers/openstack/jetstream.html#configuration","title":"Configuration","text":"<p>```yaml jetstream:   flavour: m1.small   image: ubuntu-22.04   auth: /path/to/jetstream/auth.yaml   security_group: default ```</p>"},{"location":"providers/openstack/jetstream.html#authentication","title":"Authentication","text":"<p>Jetstream requires a credentials file in YAML format. This file should contain your username, password, and auth URL.</p>"},{"location":"providers/openstack/jetstream.html#examples","title":"Examples","text":"<p>```bash cmc vm set jetstream cmc vm start --name jetstream-node-1 ```</p>"}]}